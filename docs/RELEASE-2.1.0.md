# v2.1.0 账号学习与排版

人工改稿不再走独立的五条抽象规则提炼，而是运行原版 wewrite-learn、learn-edits 差异工具与类型化模式，使用上游聚合函数生成 playbook。人工确认、栏目隔离、停用、撤销、明确改稿对和账号版本保护保留。修改规则重置学习次数，保留来源。

范文使用上游本地提取，最多两份相关真实片段，记录所有权；历史范文无需新增付费处理。账号定位、历史和学习记录写入每个任务的原生文件。

排版接入上游 make_paste_safe、validate_html；清洗保留 leaf。封面独立，不再默认插正文。保留原来的主题下拉、颜色圆点、参数与手机预览。

验证：正式目录共563项用例；全量562通过、1项旧封面断言随后按独立封面语义修正并定向通过；最终原生执行／学习／账号36项通过；前端构建、完整离线浏览器流程通过。浏览器使用模拟模型，真实执行工具与界面，不代表已经证明文笔提升。新增付费请求0。

扩展能力在第三批交付。
