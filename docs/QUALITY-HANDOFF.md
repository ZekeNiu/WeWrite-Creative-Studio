# 质量优化接续记录

更新：2026-09-23继续实施。已完成交接指定的四份既有结果复核，并恢复同配置真实调用。本文与本地session的最新记录取代旧运行状态；历史实现与失败记录仍保留，不得从最初候选重做或丢弃已有功能。

## 读取顺序

1. 主工作区 AGENTS.md：持续有效的发布、隐私与界面约定。
2. docs/QUALITY-ROADMAP.md：四批范围及不可降低的门槛。
3. 本文：最新状态、实现及交付边界。
4. 本地、不提交的 output/diagnostics/quality-continuation-20260921/HANDOFF-20260923.md：准确结果目录、失败响应、待复核/重试队列、工具用法、最终提交及检查记录。

## 当前结论

- 第一批v1.6.0已发布。第二批仍为v1.7.0候选，真实验收未通过；第三批v1.8.0候选保留，六篇用户复核未完成；第四批未开始。
- 严格顺序：第二批真实验收与发布 → 第三批六篇匿名比较、用户本人复核与发布 → 第四批实施。不得提前发布或切换候选。
- 2026-09-23核对时，正式端口8765未监听，正式服务未运行；主工作区代码仍为v1.6.0。正式数据库无running/queued任务。保留needs_input等既有记录，不清理或重写。
- 中断前的旧评测进程已结束、旧会话失效；本次新启动的v10独立诊断重试与政策22七题预检仍须按本地session、结果文件及实际进程核对，不能仅凭文档推断状态。
- 原始403响应明确为INSUFFICIENT_BALANCE；用户告知账户已恢复后，同模型同配置探测成功，后续真实调用也已完成。已修正产品中的余额不足提示。连接中断、超时、多完整结构化对象及HTTP520均保留原始失败，未完成不计通过，不换模型、路由或凭证。
- AGENTS.md保留。当前仍有未通过验收与待用户复核批次，两份质量接续文档不能删除。

## 工作区与远端

origin：https://github.com/ZekeNiu/WeWrite-Creative-Studio.git

| 工作区 | 分支 | 用途 |
|---|---|---|
| D:/AI/Wechatarticle_v1 | main | 正式代码v1.6.0与接续文档 |
| D:/AI/Wechatarticle_quality_work | codex/quality-research-v170 | 第二批研究候选，开发政策22 |
| D:/AI/Wechatarticle_editorial_work | codex/quality-editorial-v180 | 第三批候选，保持eafeeac467b7c9b1fe82d704771c0206c3064a56 |

交接开始时main为bafe21d55a861b2e0442f93d5a978d8d87a43d15，研究已提交部分为3651c34a1094573fe71a694bd2028976af0e3185；本次收尾已增加候选和接续提交，最终SHA、CI和远端核对见本地交接与Git。旧检查点不能用于reset。

两个候选的.venv、node_modules是主目录依赖联接，必须保留，不能递归删除。研究候选package/锁文件已标1.7.0，不代表正式发布；后端版本读取应用版本。

政策17提交3651c34自身CI35650019409已成功（404项、构建、dist一致、完整离线界面）。政策18完整410项、政策19完整412项通过。政策20中断前全量检查的终端结果丢失，交接时重新完整检查420项通过，完整离线界面通过，日志已保存；候选自身CI结果见本地记录，不能从进程结束推断通过。

## 第二批已有实现

保留固定用户任务书、问题轮转与渠道适配、原句及DOI/arXiv精确定位、全量候选筛选、预算内引文追踪、章节与逐源笔记、真实阅读范围、摘要/全文/身份区分、引文和语义支持分离、人工决定与支持状态分离、模型及实现缓存指纹。

政策14—17已保存：主张和boundary全部事实/数字/条件核验；独立前提、例外、判断主体及候补顺序对照；覆盖说明及自动问题解决说明从核实证据投影；按用户原句检查回答完整性；纯定位不扩展为额外结果验证；已定位引文不因章节切换误报缺失；编号章节和连续回读；最多一次同材料纠正；唯一完整结构化响应验证。多完整对象、截断及非法JSON不能任选一个凑通过，原始记录与费用保留。

本次收尾保存政策18—20：

- 清单同时绑定原文条件与同来源实际回答answer_quote，允许答案在同来源boundary中，不要求恰好在同一证据引文内部。
- 独立time检查；首次/历史版本不能由当前帮助页替代。准确翻译对象层级与条件关系。
- 原问题通过两个独立覆盖核查后，额外未匹配自动主张仍是不受支持的开放建议，不扩大用户硬要求；不改变人工决定、方向决定或真实缺口。
- 每三条证据分别核查，保留对应来源实际已读原文，移除旧AI判定和自由笔记暗示；所有证据仍全部检查。
- 覆盖核查按相同可选证据池分组。一手问题不收到只可用于其他问题的二手证据编号；不猜测映射编号。
- 每个清单编号独立对照；确定性检查拒绝跳号、重复或多项合并。编号检查不能代替语义完整性证明。

修正仍无完整真实通过记录，模拟检查数量不等于质量达标。

继续实施增加政策21—22：覆盖核查只合并被请求的问题，保留完整用户原句；未定位引文的一次修正可获得同来源实际已读的连续原文，不能跳过脚注拼接或自动替换。完整性肯定说明从已核实回答生成，相同claim_id不能解决更宽的主张；作者解释/转引作为依据类型显示，不将系统提示混入原文边界，缺少实际边界仍拒绝采纳。440项模拟检查与完整离线界面通过；对应提交及自身CI见本地记录。

## 真实验收现场

固定题集tests/fixtures/quality_cases.json的12题、目标与查询没有替换。tools/quality_benchmark.py只读正式配置，每次写入独立数据库与输出。私有根目录为主目录output/diagnostics/quality-continuation-20260921/。

- 冻结v10为06e088ec03e0ef1df4bab3c66c7aea62dd62168e，已产品失败，只用于完成诊断收集。不能发布或混入下一次最终成绩。
- 已知12题均完成，8题原文复核通过；SPRINT条件遗漏、CRISPR摘要补实验设置、Bell倒置p值、M1跨指标借脚注失败。
- 两盲轮原任务均结束。计入独立外部重试后，每轮11题取得完成结果；第一轮Nordic及第二轮CRASH最新重试访问拒绝，仍未完成。另有新完成结果待复核，准确队列见本地交接。
- 盲测还暴露观察/假说变因果、可能性变必然、讲义当原始研究、当前功能混进首次发布、清单漏定义/前提等问题。
- 政策18预检SPRINT虽自评充分，仍漏条件且把同一家庭缩成同住一室，明确失败。政策19 Bell边界把p值写成零假设拒绝概率，仍失败。sufficient=true不是人工事实复核通过。
- 移除旧判定、每三条检查的同材料对照捕获了Bell、Spanner、M1、CRISPR等实质错误，但仍不能当全量验收。
- 交接待审的v10 CRISPR与Bell均经原文复核失败：安全性扩张/临床终点边界缺失，以及读到原文却未完成机制与统计回答。旧v10仍不能用于发布。
- 政策20 Bell/M1仅通过已有的文献定位预检，未生成旧p值/15x主张，不证明这些旧缺陷已消失。重试CRISPR准确保留摘要读取边界；SPRINT/Middle暴露分组与引文问题；ACL虽自评充分，仍在肯定说明和更宽问题中加入缺据结论，失败。Chrome多份完整对象的响应保留为未完成。
- 政策21 Middle同材料结果逐条原文复核通过，SPRINT超时未完成。政策22重新进行完整七题同材料预检；不能拼接不同政策的通过项，也不能把给定材料预检计为发现成绩。

计分器v4严格核对原始身份、作者PDF及期刊首页DOI/题名/摘要，排除二手题名、教学和参考文献误命中。184份历史及当时结果已统一重算，之后新增结果不一定在其中，不能沿用旧分母。旧版历史校正9/12比较下限和每盲轮至少10/12均不变；更严格可读原稿重算7/12不用于降低门槛。

## 下一步顺序

1. 复用本次已通过且代码未变的440项和完整界面检查，增量核对提交、CI、活动评测与服务状态。
2. API恢复已确认；复核已生成的政策22结果，外部未完成项独立重试，历史失败不覆盖。
3. 完成v10剩余两题诊断及当前同实现七题预检；检查新核查既减少漏检，也不把正确资料误判不足。产品失败找根因，不针对题目硬编码答案。
4. 代码和预检稳定后上传候选并确认自身CI；修正本地过时的冻结辅助脚本，再冻结全新完整36题：已知12、两盲轮各12。两轮最终成绩来自同一实现/题集/计分器/实际模型/检索参数。
5. 已知全部正确；每盲轮≥10/12且不低于9/12。核对来源身份、实际阅读、支持强度、原意保持和错误充分。外部失败未完成不能通过，产品代码改变后重新冻结。
6. 通过后才发布v1.7.0，再推进第三批。目前不得创建v1.7.0/v1.8.0/v1.9.0标签或Release。

## 模型与参数

不换模型/路由。研究/资料继承sources的gemini-3.8-flash（chat），原生搜索同模型用gemini协议；大纲/写作/修改claude-opus-5；审核gpt-5.6-sol。实际继承、请求/返回标签和校验值在本地清单；供应商内部路由不能仅凭标签完全证明。

研究服务输出上限此前经长上下文截断诊断、备份和配置校验，已明确从8000改为16000；共享该服务的阶段也继承此额度。写作/审核仍8000。模型、温度、12次搜索/32页/4轮预算未变。8000/16000结果不能混算，不再次擅改配置。当前设置校验值与政策20预检一致，费用未知不能写成零。

## 第三批及第四批

第二批正式发布后，将最终研究修正合入既有编辑候选，保留论证综合、独立事实核查、整体编辑、差异及采用/拒绝、两轮自动上限、稿件版本和并发人工编辑保护。旧候选319项及界面通过，不能充当合并后的检查。

ACL、SPRINT、Middle、Spanner、CRISPR、M1六题；同材料、任务书、用户模型。冻结材料包SHA256为ad21847e9484f7c65462b12e58a4484418834b55cbd6676481b122e07d65f7a0。旧Middle/Spanner仅在代码、材料、实际模型一致时复用；失败题独立重试，不跳过或猜修引用。随机A/B稿、五维评分及重要事实逐条证据表，映射单独保存，先评后揭盲。

六篇必须由用户本人复核，助手/模型评分不能代替。≥4篇改善，其余无实质退步才发布v1.8.0；等待用户时保持候选。回归人工编辑保护、采用/拒绝、定稿、引用、导出和恢复。

第四批按路线图：单公众号、偏好按栏目隔离；历史与账号定位、明确AI/人工稿对的软学习、范文只学表达、效果手动/CSV及至少5篇可比样本。实际接入选题/写作/编辑并显示使用记录，撤销立即影响后续调用，运行中旧上下文结果只作待处理候选。不能只验管理页面。

## 发布、隐私与文档

每批同步package、锁文件、后端实际版本、CHANGELOG；前端变化构建提交dist。适用检查及完整离线界面通过，上传正式提交，确认该提交自身CI成功后创建新标签与非草稿Release，核对origin/解引用/发布。不强推、不覆盖旧标签。

切换前核对活动任务和备份，有任务等待；安全切换后核对实际服务、资源/dist、启动器复用和私有记录，提示旧页刷新。排版下拉框、颜色圆点、参数输入和手机预览保留。私有数据、文章、凭证、评测材料和本地诊断不提交。

历史CI纠正：v1.6.0发布提交1bd2ef69c7bfbb33f384b72de565c4a973badf1f自身曾因界面等待超时失败；后来仅文档变化的b9820ee/a8f9b63用相同代码通过，不能写成发布提交自身成功。

所有批次通过并交付后，结论归入版本交付记录，删除QUALITY-ROADMAP.md及QUALITY-HANDOFF.md，修复引用并上传清理提交。AGENTS.md持续有效，保留。当前未完成，保留并更新接续文档。
