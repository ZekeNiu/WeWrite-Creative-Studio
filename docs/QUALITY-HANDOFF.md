# 质量优化接续记录

更新：2026-09-23。本文只保留当前有效状态与后续边界。此前各政策的完整记录保留在Git历史，以及本地、不提交的 `output/diagnostics/quality-continuation-20260921/`，不得删除失败或复用旧进程状态。

## 当前结论（2026-09-23新对话，政策36候选）

第二批仍未发布。新对话已完成必要增量核对：三分支与origin一致，原配置与冻结输入未变，正式服务无监听、无running/queued任务。当前实现已从政策35改为政策36，旧冻结仅作历史，后续真实验证必须固定新实现与输入。

复用隔离SQLite事件与原始请求确认：模型已收到翻译页真实URL，却仍误称官方；不能把这个语义错误说成当时完全没传URL。另有两个通用实现缺口：审查上下文省略实际读取地址及已有身份记录；来源归属变化没有使证据判断缓存失效。

本次只做发布前最小调整：传递original_url/read_url/access_scope、身份匹配和元数据出处；材料依赖与逐条证据缓存绑定这些信息；在已有来源规则中区分原始发布者、托管副本及论坛发言。没有新增核查层、重试、题目或域名例外。重复全面审查调度和其余提示词去重继续留在第三批。

11项新增模拟回归先复现失败，修复后相关69项通过；全套507项后端及完整离线界面通过。前端、dist、模型、Key、路由、研究参数和正式数据库未改。这些检查只证明上下文传递、缓存及功能行为，不证明真实来源归属问题已经解决。

用户随后明确授权仅第二批累计76次、单题20次：先对已有SQLite材料做最多2次定向归属核查，再做2道同实现新题；失败即停止，不自动整题重跑。两次归属核查已分别将翻译页判为secondary、官方原页判为primary，但对旧主张的物理主机措辞判断有分歧，原始返回保留，不能据此宣称全部语义准确。它们只是诊断，不计新题验收。

第一道新题取得原始论文全文，但在20次内未完成用户要求的外推边界说明，第21次在HTTP发出前阻断。整体状态为未完成，并未产生可验收的sufficient=true。复核也发现表达语调要求被卷入证据缺口、否定边界留在issue而未成为核实证据、条件对照自行省略实际主张等问题；保持原始失败，不猜修引文。第二道新题未启动。当前累计56/76、尚余20，费用未知；依已授权的失败停止规则，未用剩余额度绕过前题启动后题，未提高单题上限。第二批仍未发布，第三批尚未合并，正式应用仍1.6.0。

本地续检记录：`output/diagnostics/quality-continuation-20260921/source-identity-continuation-20260923/offline-findings.json`；后端与界面日志位于研究候选的`output/diagnostics/continuation-policy36-tests.txt`和`continuation-policy36-ui.txt`。实现提交`ec63644cef93d254d3f26d328f291ea7ed05f856`自身CI`35827296762`成功；主分支仅文档提交`8df86831c552d4d4e26003054d471a81650b584a`自身CI`35827302168`成功。后续结果文档提交与自身CI以更新后的机器检查点为准。新增诊断、冻结输入、授权、台账汇总及失败复核均在上述私有目录的`source-identity-continuation-20260923/`，不得提交。


## 上一对话记录（政策35，保留失败与历史）

- 历史收尾说明（仅指上一对话）：用户最后明确允许结构性调整交给第三批，上一对话据此收尾并交接。这不表示第二批已经验收通过：第二批仍须完成必要的真实正确性验证后才能发布。重复审查调度与提示词职责收拢列入第三批；若其中某项被证实直接阻断第二批正确性，只先处理发布必需的最小修复，不以批次归属为由发布已知错误。

- 第一批v1.6.0已发布，主工作区应用仍为1.6.0。第二批v1.7.0候选尚未通过当前真实验证，未发布。第三批v1.8.0候选保留，成稿验证未完成（本人复核及匿名比较流程均已取消）；第四批未开始。
- 用户已同意“重点看通用性和少量新题”，取消完整36题强制门槛。旧七题预检及v10诊断已停止，不按旧门槛自动重跑。历史分数和失败仅用于诊断。
- 研究政策33已完成512项后端检查和完整离线界面，其提交a118d5739305bb5569ef6b21cebb9899c5cc3e57自身CI35821946850成功。模拟检查和CI不代替真实质量验证。
- 新验证固定三题：一题已知官方来源、两题新发现。第一题暴露词表将有效的否定推断误判为语气强化，13次请求后主动停止，记录为产品失败并中断，不能计通过；此时另外两题未启动。随后三次同配置历史请求对照，以及收敛后新发现题共消耗34次请求，费用未知。后一个新题达到18次单题上限仍未完成部署问题，记录为未完成，不能计通过。最后一道新题未运行，40次总上限尚余6次，不为凑题启动半轮测试。
- 政策35收回定性词表的自动语义裁决，保留原文/主张定位、独立语义判读、数值时间边界和已验证的物理量错配校验。政策34曾尝试同时删除物理量校验，真实历史对照漏掉力/应力混淆，失败结果保留；不能把删规则一概当改进。496项后端检查和完整离线界面通过；测试减少源于撤去只验证词表行为的项目，原始反例已保存并用于真实语义对照，不把删除项目计为通过。其实现提交d2058f6807b1ef56ef4764d927102be2f5f9afb0自身CI35823688001成功；本轮真实验证未通过，无活动付费进程，正式服务未切换。最终文档提交与现场以本地新交接和Git为准。

## 工作区

origin：https://github.com/ZekeNiu/WeWrite-Creative-Studio.git

| 工作区 | 分支 | 用途 |
|---|---|---|
| D:/AI/Wechatarticle_v1 | main | 正式代码和接续文档 |
| D:/AI/Wechatarticle_quality_work | codex/quality-research-v170 | 第二批研究候选 |
| D:/AI/Wechatarticle_editorial_work | codex/quality-editorial-v180 | 第三批候选，保留eafeeac467b7c9b1fe82d704771c0206c3064a56 |

两个候选的.venv、node_modules是主目录依赖联接，必须保留，不能递归删除。研究候选package/锁文件1.7.0不代表已发布；后端版本读取应用版本。

交接时正式8765未监听、数据库无running/queued任务。切换前必须重新核对。保留needs_input等原有记录，不清理数据库来制造空闲状态。

## 第二批实现与通用性边界

- 固定用户任务书，按问题轮转检索；适配网页及学术渠道，筛选全量候选，在原预算内追踪引文、按章节阅读并保存逐源笔记。
- 分别核对来源身份、真实阅读范围、连续原文、事实支持与用户问题完整性。纯书目定位不能证明实验结果；摘要不能当全文；人工继续不代表证据充分。
- 独立证据核查保留对象、数字分母、因果、时间、适用条件及不确定性。汇总与自动问题解决说明从核实结果生成，不能绕过核查增加事实。
- 同源最多4条证据共用原文，每条仍需独立且唯一的支持与条件判断；缺失、重复和错配判断拒绝。书目身份使用同源实际字段原值，正文条件使用实际已读正文。
- 已读材料最多一次修正；失败条件和真实原文作为反馈，补充候选仍须独立核查。不能增加修正次数、猜改引用编号或把未支持的自动主张变成用户新要求。
- 已完成但结构无效的响应按同一原始输入最多请求三次，逐次保存。连接、权限、余额错误不自动重试。唯一完整结构、取消与原始失败记录保持。
- 不再用固定措辞白名单决定可能性、否定或外推是否正确，交由原有独立逐条件审查；没有新增审查层或重试次数。确定性的来源定位、字段绑定与数值/物理量约束仍保留。语义模型仍可能漏错，不能用模拟数量或同题反复通过证明泛化。

政策33对历史调用离线重分组，证据核查可由92次合并为59次且不丢条目；这只是结构估算，不是已测费用降幅或质量提升。少量新题也不能声称总体准确率。

## 历史失败与私有记录

本地根目录：`output/diagnostics/quality-continuation-20260921/`。

- HANDOFF-20260923.md、session.json及各检查点记录完整历史；旧会话ID仅作追溯，不能直接复用。
- v10（06e088e）已产品失败。交接指定的CRISPR/Bell重试已复核失败；政策20 Bell/M1仅通过定位范围预检。后续真实结果及失败均保留在review-policy系列文件，不拼接为当前成绩。
- 多轮诊断发现条件遗漏、可能性强化、分母/指标错配、摘要补实验设置、核查引文自行缩写和错误充分；对应原始响应保存。政策31仍有Bell误拒，以及SPRINT/Chrome外部失败；不能写成整轮通过。
- 原403已查明INSUFFICIENT_BALANCE。用户恢复账户后原配置探测成功；后来520/521失败也保留。费用缺失为未知，不是零。主动停止时未返回请求和费用可能缺失，这项不确定性保留。
- acceptance-policy-revision-20260923.json记录用户验收修订；generality-cost-audit-20260923.json及policy33-offline-batching.json记录成本与通用性复核。
- compact-freeze-20260923.json固定当前实现、私有输入、配置与上限；compact-validation-20260923保存结果和逐题复核；compact-request-ledger-20260923.jsonl保存实际付费请求次数。统一输入字段使用mode=blind，但仅其中两题是盲发现，不能混淆。
- 旧freeze_current.py完整36题入口已停用；不再要求七题全过才冻结，不启动旧队列。
- 本轮完整接续提示词为CONTINUE-PROMPT-20260923.md，最终现场为HANDOFF-NEXT-20260923.md与handoff-next-checkpoint-20260923.json（均在上述私有目录）。优先读最终现场，不把旧HANDOFF中的活动状态带入新对话。

关于上游skill、应用研究流程与提示词/代码职责的复核，见[实现复核](QUALITY-DESIGN-REVIEW.md)。

## 接续顺序

1. 新对话先读最终现场，增量核对分支、进程、配置和请求台账；复用已保存的失败，确定第二批最小必要验证。第一题失败、第二题达上限未完成、第三题未开始，不组成通过成绩。旧36题、七题预检和v10队列保持停止，不自动扩额或整题重跑。
2. 重复全面审查的调度和重复提示词整理由用户允许移交第三批；来源身份及错误充分仍是发布前正确性要求。不要把允许延期优化理解为允许忽略已确认的事实错误，也不要在本对话继续大规模改造。
3. 第二批小样本完成且无实质缺陷，适用回归和完整离线界面通过后，完成版本记录、正式提交自身CI、标签与Release并安全切换。代码未变的可靠检查不重复。
4. 第二批正式发布后将最终修正合入现有编辑候选，处理移交的结构调整，并按下文简化后的实际成稿与功能验收发布第三批。本对话只交接，不启动第三批。
5. 第三批通过并发布后，再完整实施第四批。

## 模型与输入

保持现有Key、模型、路由和参数。研究/资料继承sources的gemini-3.8-flash（chat）；原生搜索同模型用gemini协议；大纲/写作/修改claude-opus-5；审核gpt-5.6-sol。研究服务16000输出上限、12次搜索/32页/4轮不变；写作和审核8000。旧8000研究结果不混算。

实际继承、请求/返回模型和配置校验值保存在私有记录；供应商内部路由不能仅凭标签完全证明。有限验证的调用上限不是产品参数变化。

## 第三批及第四批

第三批保留论证综合、独立事实核查、整体编辑、差异与采用/拒绝、两轮自动上限、稿件版本和并发人工编辑保护。旧候选319项及界面通过，不能充当合并后检查。

第三批取消用户本人逐篇复核、随机A/B匿名稿、五维评分表、逐条重要事实证据表、映射另存及先评后揭盲流程；相应的“至少四篇改善”不再作为发布门槛。ACL、SPRINT、Middle、Spanner、CRISPR、M1及校验过的材料保留为可复用回归资料，不因此要求重新生成全部六题的两套稿。采用少量有代表性的实际成稿，检查重要事实、论证、用户原意和可读性；事实核查继续做，无须另制逐条证据表。模型评分只辅助，不替代实际检查。产品内的五维审核、人工编辑、采用/拒绝、定稿和恢复功能保留。

材料包SHA256为ad21847e9484f7c65462b12e58a4484418834b55cbd6676481b122e07d65f7a0。需要比较或复用旧稿时，先核对材料、任务书、实现与实际模型的一致性；失败独立重试，不跳过或猜修引用。回归并发人工编辑保护、采用/拒绝、定稿、引用、导出与历史恢复。

移交的结构调整必须落实到实现：用已有事件检查收集材料与完整审查的时机，减少重复阶段提示；明确原始发布者、转载/翻译页和论坛内容的身份边界。优先小范围简化现有流程，不加题目/域名例外，不另造审查框架。保留公平检索、预算、取消、来源定位与独立事实核查。

第四批完整要求见[路线图](QUALITY-ROADMAP.md)：单公众号、默认按栏目隔离偏好；近30天历史及近7天加强；可编辑账号定位；明确AI/人工稿对软学习与确认固定；范文只学表达；手动/CSV效果与相同观察窗口、至少五篇才有限趋势。实际接入选题/写作/编辑并显示使用记录，撤销立即生效，运行中旧上下文只产待处理候选。SQLite增量可选字段与版本冲突保护，复用导入、取消、进度、事件和阶段路由，验收完整生成及边界流程。

## 交付与清理

遵守AGENTS.md：同步版本、锁文件、后端与CHANGELOG；前端变化构建提交dist。上传正式提交，确认其自身CI成功后创建新标签和非草稿Release，核对origin、标签解引用与发布。不得强推或覆盖旧标签。

切换前重新核对活动任务并备份，有任务等待。切换后核对实际服务版本、资源/dist、启动器复用及私有记录，提示旧页刷新。保留原排版下拉框、颜色圆点、参数输入和手机预览。

仅提交项目代码、文档、预构建界面及模拟测试；私有数据库、文章、凭证、评测输入和响应均不提交。历史v1.6.0发布提交自身CI曾失败，后续相同代码的文档提交成功不能改写这一事实。

全部批次通过并发布后，结论归入版本交付记录，删除QUALITY-ROADMAP.md和本文，修复引用并上传清理提交。当前仍未完成，保留更新；AGENTS.md持续有效。
