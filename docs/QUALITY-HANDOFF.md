# 质量优化接续记录

更新：2026-09-22。第一批已正式交付；第二、三批是开发分支的候选，真实验收未完成；第四批未开始。不得把候选版本或模拟测试通过描述为整轮完成。

## 本轮执行检查点

最新冻结 `77ce4f9` 已上传 origin/codex/quality-research-v170；`known-v6`、`blind-r1-v6`、`blind-r2-v6` 已启动全部36题，每组并发2、独立数据与输出，配置/题集/计分器/实际模型一致性校验通过。对应CI运行号35627523063，完成情况以实际结果为准。当前正式服务仍为1.6.0。

最新范围修正已通过完整354项本地检查：纯文献定位中的题名人数、作者与年份是身份线索，不再升级成用户未要求的原始数字核验；明确要求数字溯源、研究对象或结果的任务仍须实际内容证据。Nordic真实种子预检完成且充分；ACL保留数字分母与因果核查、缺据和方向问题，不误报充分。新提交仍需对应CI及同一冻结实现的全部12+12+12真实验收。

此前0566d9d、947b162、87f1dfc均通过各自代码检查，87f1dfc对应 GitHub CI [35624390149](https://github.com/ZekeNiu/WeWrite-Creative-Studio/actions/runs/35624390149) 成功；真实验收仍发现产品问题，不能发布。v3发现数字/证据支持问题，v4发现中间代码框遮蔽框外完整结果，v5发现书目识别线索被升级为额外事实核验。原始响应、费用与活动现场均保留；代码框已改为整份响应唯一性校验，不修改内容，多份完整结果仍拒绝。主工作区仍运行 v1.6.0，没有发布或切换候选。

私有续作目录的v3、v4、v5三组开发运行及v5独立重试均已结束或明确停止，不能计入下一快照的最终验收。v5 CRASH独立重试完成并找到原文，但不跨快照复用成绩；其余接口超时、流错误、截断或格式歧义均保留为未完成。下一组必须使用同一新冻结实现、原题集、实际继承模型及12/32/4检索预算，每组独立数据库与输出。进展以新冻结清单、结果JSON、隔离任务状态及私有session.json为准，不混算快照。

## 已发布与实际运行

- 基线：v1.4.12 / d8a3400。
- v1.6.0发布代码：`1bd2ef69c7bfbb33f384b72de565c4a973badf1f`。远端标签解引用、非草稿Release和main已核对。该发布提交的GitHub CI在离线界面等待“开始一篇新文章”按钮时超时失败；随后仅修改交付/接续文档的main提交`b9820ee`、`a8f9b63`使用相同代码通过CI，不能将其误写为发布提交自身通过。
- Release：https://github.com/ZekeNiu/WeWrite-Creative-Studio/releases/tag/v1.6.0
- 第一批286项模拟测试、前端检查/构建、完整离线界面流程通过；4项真实合成语义核查通过（因果、分母、机制推测、正确对照）。完整跨领域召回不是第一批已通过项目。
- 正式服务：`D:/AI/Wechatarticle_v1`，端口8765。交接前再次核对健康接口1.6.0、活动任务0，实际首页及界面资源与dist字节一致。此前已验证启动器复用进程，升级前后文章/设置/凭证原始记录未改变。旧页面需要刷新。
- 保留既有v1.5.0标签；未创建v1.7.0、v1.8.0、v1.9.0标签或Release。

## 必须保留的工作区

| 目录 | 分支 | 实现检查点 | 状态 |
|---|---|---|---|
| D:/AI/Wechatarticle_v1 | main | v1.6.0发布代码及接续文档 | 正式服务 |
| D:/AI/Wechatarticle_quality_work | codex/quality-research-v170 | 87f1dfc之后继续修正；以最新冻结清单及git为准 | 1.7.0候选 |
| D:/AI/Wechatarticle_editorial_work | codex/quality-editorial-v180 | eafeeac | 1.8.0候选，已上传origin |

检查点是实现提交，之后可能有接续文档提交，以git status/log及远端为准。两个候选目录的.venv、node_modules是指向主目录依赖的联接，不能递归删除。第三批已逐次吸收第二批修正。先发布第二批，再合并第三批；常规冲突保留最新研究修正和第三批编辑功能。

## 第二批已实现

- 结构化查询、问题轮转、渠道适配、全量分批筛选及统一排序、阅读队列、继续检索；非空但不相关的索引结果不阻止其他渠道。
- 有预算且最多两层的参考/被引追踪；章节索引、原文定位笔记、定向回读、表格/脚注/补充材料线索；arXiv同版本正文核验。
- 指定DOI/arXiv单独覆盖和精确定位；二手资料不替代指定原文，核心学术依据区分原始结果与转引。
- 解析供应商中转链接并补全文献身份；身份查询并发最多4个、单项最多45秒，失败仍保留候选；全文失败保留真实摘要。
- 原始完整要求保留一次查询，避免所有渠道受同一个规划猜测限制。必需子问题只绑定用户原句；明确采用方案独立保存，自动展开不增加硬要求。
- 独立审核整个问题的联合条件；总问题汇总经过核实的子问题证据后再审；定向任务不重开无关已核实问题。
- 当前证据政策11、任务书版本7；模型、提示词、分析实现和政策进入缓存指纹；旧结论待复核，原文可复用。停止原因区分覆盖、无进展、预算、轮次、渠道。

- 第二批当前完整354项模拟测试通过；此前前端构建及扩展界面流程通过，界面代码未改变；新提交仍需核对对应CI。真实质量门槛未通过。
- 接续修正：覆盖审查从独立核实的候选证据中选择，不因遗漏问题标签而制造缺口；非用户要求的规划问题保持可选。书目题名只支持文献身份，结果/方法/因果仍需原文内容。完整但结束括号类型错误的结构化响应可保守恢复；不改写内容、不接受截断或多解。
- 评测清单记录实际继承模型、配置/题集/计分器/实现校验值；不允许不同快照混算，私有目录保留原始请求、响应、流、失败与费用。候选分支也触发GitHub CI，正式切换前先验证。
- 核查完整主张中的数字、括号专有名词及适用条件；不从四舍五入的比例反推实际人数，不用相近子组比例冒充目标数字出处。单条证据保持一个可核对判断与连续原文。
- 文献定位中的书目身份线索按完整用户原句理解，不升级为额外结果或数字核验；用户明确要求数字分母、原始出处、研究对象与结果时仍须相应正文或摘要依据。
- 研究调用向兼容接口请求JSON schema，不变更模型、路由或输出上限；仅格式参数被明确拒绝时回退并保留双方记录。返回后仍严格校验，不假设接口必然执行schema。核查报告的列表字段改为必需，避免无关中间对象因默认空列表而被误判成完整报告；多个真正完整结果仍拒绝。

## 第三批已实现

- 大纲前论证综合；每节关联判断与依据，校验来源及证据编号。
- 独立逐段检查未引注事实、数字、具体研究结论、引语、研究条件与因果边界。
- 整体编辑候选、差异预览、独立复核和采用/拒绝；并发人工修改不覆盖；自动最多两轮，新事实错误不自动采用。
- 初稿、编辑稿、人工定稿分开记录；真实人工改稿保留原稿关系，AI编辑不冒充人工偏好，人工定稿不改变AI审核结论。
- 准确性、观点、用途、声音、可读性固定1—5分，平均至少4、各项至少3，分数不能覆盖事实错误。
- 对真实发现的字符串列表缺失左方括号，只保守恢复分隔符；不修改文字、数字、来源ID或顺序，不接受截断或多解结果，原始输出仍保存。
- 当前集成319项模拟测试全部通过。前端检查/构建通过；当前完整离线界面流程通过，覆盖真实差异、采用、定稿、保存冲突、导出和原排版布局；已检查候选差异截图。
- 六篇真实成稿比较未完成，不能发布1.8.0。

## 真实评测：结果与限制

本轮续作记录位于主目录私有 `output/diagnostics/quality-continuation-20260921/`。40d98e3、7678290及0566d9d的运行均已结束或明确停止并保留，不能计入最终验收。最新修正的真实回放正确拒绝反推分母、缺据专名及错误数字出处；保留完整上下文，因为删减上下文的探索回放仍会误判。多对象失败进一步定位为默认空列表造成中间片段误通过schema，改为必需字段后四份原始失败各有唯一完整结果，未改写内容。真实接口接受结构化参数，但仍可能返回错误字段，因此严格校验不能省略。

固定材料预检不是发现验收：政策11的Nordic摘要身份预检通过，ACL保留分母及因果核查并阻止缺据和方向问题；Middle本次回归已完成，原始位置实验、反例及适用边界均覆盖；两处不连续引文被正确阻止，结果未充分，不能将其描述为全部证据通过。前次错误根字段响应原样保留。下一步须在同一最终实现上完成全部12+12+12真实运行，不能以预检替代。

固定题集`tests/fixtures/quality_cases.json`的答案未改变。运行器`tools/quality_benchmark.py`只读生产配置，写入全部隔离；不要换模型或将外部失败算通过。

- 旧版盲发现11项执行完成；Bell因外部HTTP520未完成。原自动计分8/12，修正官方PDF题名漏计后为9/12。
- 计分器v2补充既定官方域名PDF首页题名检查，不采信教学副本或正文参考文献提及。旧版已下载Spanner的原始OSDI PDF，文件名不应造成漏计。原计分和校正记录都保留；没有换目标论文。
- 候选358efbf：SPRINT停于二手资料且误报充分，未通过。
- 候选3e73487：已知定位ACL、Nordic、SPRINT、CRASH、Middle、RULER完成并找到目标；Spanner取得目标但模型流出错，任务未完成。盲发现ACL未找到核心依据却误报覆盖，不能通过；Nordic、CRASH找到目标，SPRINT流错误未完成。
- 原始查询修正后的开发试验：ACL盲发现取得目标论文全文和另一篇神经认知错误研究；并发查询候选也在第一次搜索后读到核心全文。但这些运行早于最后的汇总修正，不替代最终全量验收。
- 较早Middle盲发现完成且定位成功；最终两轮跨领域回归仍待做。最新并发候选Middle遇到模型流错误，未完成。
- 被替代或交接时未完成的诊断已保存并明确停止，标interrupted，不算通过或外部不可用。没有遗留付费评测进程，正式任务未被中断。

成稿协议：`tests/fixtures/draft_quality_cases.json`；运行器：`tools/draft_quality_benchmark.py`。六题为ACL、SPRINT、Middle、Spanner、CRISPR、M1。两版使用完全相同材料和用户模型。材料包SHA256：`ad21847e9484f7c65462b12e58a4484418834b55cbd6676481b122e07d65f7a0`。

- 旧版ACL、SPRINT因生成不存在的来源编号未完成；Middle、Spanner完成生成，尚未匿名评判。
- 早期第三批候选ACL大纲JSON损坏未完成，已定位并补保守分隔符修复及真实原始输出复验；SPRINT流错误。Middle在事实核查时保存现场并停止，其余未完成。
- 这不是4篇改善的证据。固定当前候选后继续，保留全部失败，重试用独立目录。不得凭猜测把未知[S1]改成某真实来源；只有预先确定并验证的一一映射才可转换。

## 下一步顺序

本次接续已确认：六篇匿名稿和逐条原文证据表由用户本人完成人工复核，通过后才能发布第三批；第四批按单个公众号实现，偏好默认按栏目隔离。全部批次完成后将结论归入版本交付记录，删除本接续文件和QUALITY-ROADMAP.md并修复链接；AGENTS.md的长期交付约定仍保留。尚有未通过或待人工复核事项时不删除接续文档。

1. 核对三个目录、远端分支、正式服务和本文件，保留未提交内容；不重做上游审查。
2. 固定第二批最新快照，先验证整体覆盖汇总、原意保持、长上下文回归，再完成12个已知定位和两轮12个盲发现；每轮至少10/12且不低于更正后的旧版9/12。外部失败独立重试/记录未完成，不能算通过。
3. 修复失败根因，不把特定题目DOI、作者或答案硬编码进产品。重点检查流中断、模型来源编号、原意拆分/扩展、摘要与全文边界。
4. 第二批通过后更新CHANGELOG/交付记录，确认1.7.0、锁文件、后端和dist，按AGENTS.md提交、标签、Release及安全切换；先检查活动任务。
5. 第三批沿用冻结六题和材料包，在最新固定版本生成随机A/B稿及揭盲记录。匿名对比和重要事实逐条人工复核尚未完成，不能只凭模型总分。至少4篇改善、其余无实质退步后发布1.8.0。
6. 实施第四批：跨文章索引与30/7天重复提示、可编辑账号定位、明确原稿/人工定稿对的软学习、确认硬规则/编辑/停用/删除/时间衰减、范文导入分析与检索、手动和CSV效果反馈、至少5篇可比数据才给有限趋势，以及实际生成中的使用追踪。沿用SQLite与现有任务/模型路由，单篇要求优先，发布预计1.9.0。

## 私有记录与兼容

- 永久副本：`D:/AI/Wechatarticle_v1/output/diagnostics/quality-session-20260921/`，包含基线/候选快照、各轮结果、隔离数据库、冻结材料包及interrupted-development.json。Git忽略，禁止提交。
- 原位置仍在`%TEMP%/wewrite-quality-20260921/`。副本保留原manifest的历史路径；不要改写旧manifest假装新运行。新评测用新输出目录。
- 第一批运行核验：`output/diagnostics/quality/runtime-v160.json`。第三批界面证据：`D:/AI/Wechatarticle_editorial_work/output/playwright/reliability/`。
- 未改生产模型、凭证、个人文章、引用编号、人工决定和排版参数。新增字段增量保存、首次写入前校验数据库备份。第二/三批没有加载到正式服务。

## 新任务提示词

```text
请继续完成 D:\AI\Wechatarticle_v1 的公众号内容质量优化。
先读 AGENTS.md、docs/QUALITY-ROADMAP.md 和 docs/QUALITY-HANDOFF.md，
核对 main、D:\AI\Wechatarticle_quality_work、D:\AI\Wechatarticle_editorial_work、
远端开发分支和正式服务。以最新交接为准，不重做审查，不丢弃候选实现。

第一批v1.6.0已发布；第二、三批有候选但真实验收未通过，第四批未开始。
先完成第二批最新固定版本研究验收，再完成第三批同材料同模型六篇盲评，最后第四批。
保留所有失败，不把找到ACL一篇、模拟测试通过或模型总分视为全量达标。

质量优先，API成本可接受；只做公众号；人工改稿自动软参考，明确确认后才固定。
保留排版、引用编号、人工决定和版本恢复，不擅自换模型。
每批按AGENTS.md完成检查、版本同步、提交dist、提交/标签/Release上传和运行核验。
不覆盖标签，不中断活动任务，不提交私有记录。连续完成可高质量交付的批次；
若仍有剩余，更新接续文件与下一步，不能把候选描述为已发布。
```
