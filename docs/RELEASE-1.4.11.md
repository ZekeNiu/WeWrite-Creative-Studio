# 1.4.11 验收记录与后续检查

本版已通过 227 项后端回归、发布前 32 项针对性复查、类型检查及正式构建。公开 BMJ / PMC / PubMed / DOI / PDF 读取及另一篇公开论文验证通过；新增导入、取消、错误重试、限定应用撤销、回收站及三种窗口尺寸已用隔离浏览器验证。未调用付费模型。

按用户离线时间要求缩短最终复查，以下事项留待下一次继续：

- 补跑旧浏览器整套脚本的最后部分：长证据展开、禁用会话存储与侧栏抽屉键盘操作。此前写作选段、审核、配图上传采用及排版复制导出均已通过。旧脚本跨文章导航等待存在竞态，已调整等待方式，不能将未补跑部分记为本版通过。
- 完整创作路径脚本需适配建议已自动展开后的选择器，并补跑到底；新增建议流程的独立浏览器验收已通过。
- 对提高检索上限后的真实完整续查、限定与并发人工修改交错、旧同步文件导入在保存冲突时的附件清理，增加异常交错场景验证。

旧浏览器页面需要刷新。个人数据、凭证及诊断输出不在发布文件内。
