# 1.4.0

## 改动

- 导出先保存编辑，再生成完整归档；正文、HTML、图片、来源清单和 ZIP 使用同一快照。重复下载复用已有快照，改稿和改标题保留旧版。
- 成品、诊断产物与隔离测试目录分开；日志和浏览器缓存归入 `data/` 子目录。已知旧产物提供可重复执行的整理工具。
- 能力测试按“服务＋模型”显示，包含节点覆盖模型；文本、图片、联网分别记录结果。独立联网协议不会更改文本协议或创作分工。
- 检索默认模型优先，按证据缺口使用已启用的后备；资料足够就停止，不再固定追加学术检索。保留原文读取、引用核对、预算、缓存与取消保护。
- 素材页统一“查找并整理资料”，合并总结，折叠过程记录，明确过期状态。BibTeX / RIS 使用统一上传入口。
- 搜索配置移至流程偏好，网页搜索与动态网页读取分开控制。移除工作台搜索测试入口和人工验证浏览器流程。

## 验证（2026-09-18）

- 后端 108 项测试通过：模型独立测试、配置失效、搜索回退与限额、材料排除、取消、冲突、版本恢复、引用、归档复用及失败等。
- 前端构建通过；保留预构建 `dist/`，日常双击启动无需 Node.js。未升级项目依赖。
- 模拟界面验证：6 张卡片共 18 项能力测试；1366×768 和 1280×800 布局；资料整理至写作、审核、导出；未保存编辑导出、重复归档、刷新恢复、BibTeX 导入及过期提示。
- 独立 Edge 浏览器成功下载 ZIP，文件内容及版本名称正确，无页面脚本错误。保留用户下载管理器设置。
- 真实调用另行验证：当前联网配置取得来源并读取、核对原文；当前图片配置实际生成一张图片。测试在隔离工作区运行，未把模拟结果登记为正式模型能力。
- 启动、重复启动复用、停止、端口占用回退和重启通过；重启前后正式文章、历史版本和凭证内容一致。

## 保留的限制

- 真实验证仅覆盖本机当时选用的联网和图片配置，不代表所有服务、模型或渠道均已通过真实调用。网站访问限制和第三方服务状态仍可能影响结果。
- 云服务可能收费；费用未知时不视为免费。付费请求中断或结果未知时不会自动重复请求。
- 测试仍有依赖弃用及模拟配置序列化提示，构建仍提示编辑器包较大；本次未为消除提示升级依赖或调整编辑器架构。
- IDM 等工具可能接管浏览器下载；成功导出的文件同时保存在本机归档目录。单独 HTML / Markdown 需配套图片目录，完整作品建议使用 ZIP。

本机文章、素材、凭证、测试截图及报告均未纳入公开仓库。
