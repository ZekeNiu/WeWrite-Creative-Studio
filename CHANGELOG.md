# 2.3.0

- 统一选题执行入口，未采用主题、探索反馈和检索要求保存为文章输入草稿；保存、运行和采用分离，保护切页及刷新前的输入。
- 增加安全的结构化服务错误、生效模型提示、阶段准确的失败卡与重试参数；保留原超时，不增加自动重试或模型切换。
- 分离内容状态与最近任务状态，显示当前动作及请求数；隔离跨文章旧响应，修正扩展完成后文章版本尚未同步的问题。
- 优化文章库、素材层级、大纲折叠／移动／撤销、编辑入口、审核意见、独立篇幅设置、图片关联、窄屏导航和可读性；保留七阶段、侧栏和原排版控件。
- 离线检查与真实浏览器验收覆盖完整流程、失败恢复和三种宽度。真实选题验证发出10次请求，均收到响应，但限额内未产出候选；按约定停止，不宣称真实选题已经跑通。见 [交付记录](docs/RELEASE-2.3.0.md)。

# 2.2.0

- 增加独立扩展入口：学习排版、自定义人格、小红书／抖音改写、公众号文章／图片帖草稿箱、微信草稿副本、在线效果回填与数据复盘。
- 多平台执行完整技能与平台规范，源稿只读，使用真实上游质量／相似度工具；未达要求保留待处理，最多两次重试。
- 公众号凭证加密并隔离于模型；推送绑定当前预览、文章／账号版本和凭证，幂等任务不重发。取消等待回执，重启停止后续请求并恢复迟到回执。
- 效果数据按明确微信 msgid 关联，缺失不补零；保留原始数据，进入同一原生历史。旧稿、人工决定及既有排版界面保留。
- 阅读建议改走上游执行层，删除默认调度中不可达的旧创作／重复编辑分支；搜索限额沿用本文设置。
- 正式目录573项用例：全量572项通过，后续恢复及受影响模块定向56项通过；构建、完整离线界面和最终扩展界面通过。新增付费请求0；真实模型文笔、公众号权限和真实推送效果不以模拟验收代替。见 [交付记录](docs/RELEASE-2.2.0.md)。

# 2.1.0

- 人工改稿执行上游学习技能、真实 diff 与类型化模式；置信度和重复提升使用上游汇总，保留栏目、确认、撤销和并发保护。
- 范文导入无需模型；每篇最多提供两份真实表达参考，记录所有权及私人材料不可复用标记。账号映射到原生风格、历史、学习和范文文件。
- 补齐上游粘贴加固和排版兼容性检查；封面独立导出，配图按需；原排版控件及手机预览保留。
- 搜索／原文读取限额跨原生阶段累计；来源别名归回稳定编号。
- 563 项正式后端用例覆盖（全量后修正封面旧断言并定向复核）、构建与完整离线界面验收；新增付费调用 0。见 [交付记录](docs/RELEASE-2.1.0.md)。

# 2.0.0

- 默认执行完整上游技能与多轮工具调用；按需补读原文，运行真实上游命令，固定提交及文件校验清单。
- 创作默认移除强化研究、论证综合、分段核查和额外改稿循环；原生主张不再被旧核查字段重判。
- 上游编辑报告与实际稿件绑定，最多两轮，轻微建议不强制改写；保留候选、引用编号、取消及文章／账号版本保护。
- 增加工具调用能力测试、全部付费调用的总请求／金额限制、执行记录和任务产物查看；测试只收集正式目录。
- 560 项后端检查、前端构建及完整离线界面通过。新增付费调用 0；未完成真实质量对照，不将模拟验收解释为文笔提升。见 [交付记录](docs/RELEASE-2.0.0.md)。

# 1.9.0

- 增加单公众号账号定位和完整主题／角度／读者问题／结论／状态索引，近30天重点、近7天加强，允许同主题新证据和新角度。
- 仅从明确且不同的AI稿／人工定稿对提炼本栏目软偏好，保留来源、独立对数和时间衰减；确认固定与扩大范围分开，支持编辑、停用、删除和撤销。
- 范文只提炼表达结构，原文不进入写作上下文。手动／CSV效果按文章和同观察窗口记录，缺失不记零，至少五篇才给有限描述。
- 实际接入选题、大纲、写作、选段修改及整体编辑，展示实际提供的参考。运行中账号变更保留待处理候选，保存与采用时再次保护；文章恢复不复活账号偏好。
- SQLite校验备份后增量迁移，不改旧正文、引用、编号或人工决定；沿用任务、取消、事件、阶段路由和事实核查。未改用户配置，未进行本批付费调用；实际限制及验证见 [v1.9.0交付记录](docs/RELEASE-1.9.0.md)。

# 1.8.0

- 大纲前建立论证综合，记录核心判断、支持链、最强反方、冲突与边界，并验证来源对应。
- 成稿逐段独立核对数字、研究结论、引语及未引注的重要事实；固定五项1—5分门槛，分数不能覆盖事实错误。
- 支持整体编辑、独立候选、差异预览与采用/不采用；自动模式最多两轮，新事实错误不会自动覆盖原稿，并发人工修改保留候选。
- 初稿、编辑稿与人工定稿分开记录，保存真实人工修改的原稿关系；人工定稿不改变AI审核结论。
- 合入v1.7.0最终来源上下文、缓存及事实核查修正。检索按一轮问题和每两条延期来源集中评估；正文审核直接执行已有独立事实核查，不再先重跑完整研究。
- 删除阶段提示中与系统重复的主张、数字和定位规则；完整性核查只处理需要资料回答的要求，表达语气与篇幅交给成稿编辑。事实核查只接收原文、任务和当前段落，不接收旧AI自评。
- 523项后端检查、前端构建与完整离线界面通过，实际回归采用/拒绝、人工定稿和历史恢复。实际ACL部分改善但仍需人工修订；M1因服务断连未完成编辑，累计69/76后停止。不要求六题两套稿、匿名评分或模型逐题全通过；详见 [v1.8.0交付记录](docs/RELEASE-1.8.0.md)。
- 明确公众号表达目标，事实限定自然融入句意；删除上游文件创建和重复交付指令，不强制每篇展开机制、反方或局限专节。保留原题、人格、引用与事实核查；最终提示调整通过离线检查，未在外部失败后追加付费生成。

# 1.7.0

- 按用户核心问题轮转检索，适配网页和学术渠道，筛选全量候选，在既有预算内追踪引文、按章节回读并保存带原文定位的逐源笔记。
- 分别核对来源身份、摘要/全文范围、连续引文、事实支持及回答完整性；保留数字分母、时间版本、因果强度、适用条件和不确定性。书目身份使用来源实际字段，不能替代实验结果证据。
- 同一来源最多4条证据共用核查原文，每条仍须独立且唯一的支持与条件判断，减少重复上下文。已有核查缓存绑定原文、主张、边界、模型与实现，缺失或错配判断不能算通过。
- 汇总及自动问题解决说明来自核实后的证据，不另加缺据事实。核查用户原句，不把额外机制或后续实验扩成新硬要求；人工继续与证据充分分别表示。
- 已读材料最多一次修正，反馈具体缺口和实际原文；补充候选仍全部独立核查，不猜补引用、不拼接不同来源、不增加检索预算。
- 结构化研究响应须有唯一完整结果；对已完成但结构无效的响应按原输入最多请求三次，保存逐次原始请求、响应与费用状态。取消、连接故障、权限及余额错误不自动重试；余额不足提示更准确。
- 研究轨迹区分搜索请求、供应商内部查询、读取和元数据活动，保留候选处置与阅读范围；原引用、人工决定和排版布局继续保留。
- 撤去按固定词语推断可能性、否定和外推关系的自动裁决，交由原有独立逐条件核查；保留原文定位、字段绑定、数值时间边界及物理量错配校验。未增加核查层或重试次数。
- 补齐原始入口、实际读取地址、访问范围和元数据出处的审查上下文；来源归属变化使相关材料依赖及证据缓存失效。来源规则区分原始发布者、公开副本、翻译/转载和论坛作者，不新增审查层。
- 507项后端检查及完整离线界面通过。定向归属诊断区分了译文与官方原页；真实新题读到原始论文全文，但达到调用上限仍未完成，下一题依约停止。保留全部失败，不将模拟检查或少量样本表述为普遍准确率。
- 按最新验收原则，检查确定性的功能与保护、合理提示词和有限的真实表现，不以模型在每道题上完全成功作为发布门槛。未完成仍为未完成，不改成资料充分；重复审查和提示词职责收拢进入第三批。详情见 [v1.7.0交付记录](docs/RELEASE-1.7.0.md)。

# 1.6.0

- 第一批质量优化：稳定任务书、逐问题证据覆盖、独立语义核查，区分可创作与已充分核实。
- 修复核心引文失败降级、缺口丢失、替代全文身份错配和动态摘要误标；缓存纳入分析模型及核查版本。
- 保留原文、引用、人工决定与排版布局，增加旧证据待复核提示和升级前数据库备份。
- 后端286项测试、前端构建、完整界面验收及四项真实模型语义样例通过。完整跨领域召回、成稿比较和后三批功能仍在进行。
- 详情见 [v1.6.0交付记录](docs/RELEASE-1.6.0.md) 与 [四批最终归档](docs/RELEASE-1.9.0.md)。

# 1.4.12

- 修复连续保存覆盖和错误成功提示：配图提示词、图注、采用状态、排版参数等按最新文章排队合并，失败保留输入并可重试。
- 历史恢复替换完整业务状态，清除快照中没有的后来字段；恢复可撤销，活动任务期间拒绝恢复。新历史采用带校验的无损压缩，兼容旧 JSON，首次写入前建立并校验数据库安全副本。
- ZIP 改为文章分享包，仅包含正文、排版、采用图片及正文实际引用的文献信息。来源字段使用白名单，排除私人全文、备注、使用要求与本地路径；新归档格式为 share-v2，旧归档保留。
- 全部设置与凭证在验证后事务提交；错误服务引用明确拒绝保存。组合引用中的未知编号统一校验；Word 保留段落、标题及表格顺序，未识别图片和扫描内容明确提示。
- 当前主张证据统一生成来源详情、摘要和写作上下文。定向核实保留无关主张；目标没有有效新证据时不沿用旧核实结论。原文定位与支持强度分开，缺少评估信息标为待复核。
- “本篇不使用”与限定表述共用局部改稿及撤销：支持删除、开始时与当前双重定位、重叠及人工修改保护，记录已应用／部分应用／待处理／无需修改。最多每组 10 项，中断保留完成组，不自动重试模型请求。
- 普通局限作为写作条件，处理回执展示实际改动和未完成数。本文检索上限持久保存，优先级为本次覆盖、本文设置、全局默认；任务停止与缓存包含指定问题范围。
- 文章库标题／主题搜索及每页 20 条，历史每页 50 条；接口无分页参数时保持旧形式。清理到期研究缓存，不删除历史版本和附件。
- 启动器按依赖锁文件指纹安装并自检，失败不更新标记，继续保护活动任务；新页面使用随机唯一标记，兼容低精度系统时钟。新增 Windows 自动检查，全部后端测试隔离临时数据库并禁止未模拟的外部 HTTP 请求，界面使用模拟模型和离线浏览器。
- 保留创作目标、篇幅、资料渠道及排版下拉框、色点、参数和手机预览布局。完整备份恢复界面、上游能力补齐和真实内容质量对比留待后续；模拟验收不代表达到上游质量。
- 验证明细与升级注意事项见 [v1.4.12 交付记录](docs/RELEASE-1.4.12.md)。旧页面需刷新。

# 1.4.11

- 修正 PMC 文献身份查询，链接与 PDF 共用公开文献识别和全文读取；优先稳定标识，验证页不冒充正文。上传保留原文件名，按文件摘要及已核对身份去重，识别失败仍保留可读正文。
- 链接及文件采用可查询、取消、重试的后台导入任务，默认总时限 90 秒；弹窗内显示进度及错误，完成后明确材料名称、取得范围、关联建议和“核实这批资料”入口。
- 素材页改为紧凑状态、当前结论和统一优先级建议列表；旧记录缺失统计不再显示空数字。保留人工处理决定及定向核实范围，建议不阻止进入大纲或写作，未获支持的断言仍需限定或省略。
- “采用限定表述”按已有材料定向生成，准确定位后局部应用大纲与正文；提供改动记录和撤销，无法定位或依据变化时保留原内容。无稿件时保存后续写作约束。
- 检索设置支持任务级搜索 1–100 次、页面 1–500 个、补查 0–20 轮，可选择用于以后任务；文献信息查询单独计数，调高上限保留历史和已有材料。
- 文章库支持移入回收站、恢复及确认后永久删除；活动任务存在时拒绝删除，永久删除仅处理该文章管理的附件、历史及记录。
- 验证：227 项后端回归通过；真实 BMJ、PMC、PubMed、DOI 和 PDF 均识别同一论文并取得正文，另一个公开论文样例通过。隔离浏览器验证导入失败重试、取消、补充关联、限定应用撤销、回收站及 1440／1100／390 窗口；写作、审核、配图和原排版布局回归。所有 AI 验收使用模拟服务，无新增付费模型请求。
- 更新后旧页面需要刷新；个人文章、数据、凭证及本地验收记录不随版本提交。

# 1.4.10

- 保存文章级创作意图、最近三批候选及反馈；反馈同时进入检索与选题，采用完整方案并传入大纲、正文和审核。候选明确作为调查方向，按环节整合 WeWrite 任务书、框架和内容深化规则。
- 主张与原文支持关系采用一份当前记录，保留检索任务快照。定向核实按稳定问题／主张 ID 合并；篇幅、人格、语气、排版和未采用素材不重开事实问题，相关来源变化才更新关联主张，新增采用材料先增量分析。
- 素材页按当前结论给出主要操作；区分必须处理、写作边界和已处理。用明确限定表述或本篇不使用替换“忽略并继续”。资料关联随上传提交，取消不留粘性状态；保存失败保留输入，后台核实保留之后的人工正文。
- 先检查已有材料再联网；无进展及达到上限时停止，重复请求幂等。任务执行完成与资料是否够用分别表达；移除重复的通用缺口，清理失效续行关系，自动模式仅续行有效原任务。
- 手动链接与自动查找共享论文读取服务，保留发现片段和身份信息，区分书目、摘要与全文；补充 Europe PMC XML，并以稳定标识核对公开副本。真实 BMJ 原始链接在出版社返回 403 时成功识别 DOI／PMCID 并取得公开全文。
- 验证：211 项后端回归、类型检查和前端构建通过；隔离浏览器覆盖新增资料完整路径、七环节、保存保护、侧栏、选段修改、审核、图片上传采用、排版复制及 ZIP，检查 1440／1100／390 窗口。
- 内容对照仅做一组固定公开材料、相同人格和模型的基线／新版测试，共 8 次文本请求，无失败重放、图片生成或文章发布。观察到候选角度差异和立意延续；两版仍存在过度因果表述及超篇幅，不能据此宣称内容质量全面提升。随后补强通用证据规则，未重复付费生成。

- 实际后台与浏览器提供的界面已核对为 1.4.10；空闲时安全切换、启动器同版本复用通过，个人文章、历史、设置和加密凭证记录保持不变。旧页面需要刷新。

# 1.4.9

- 右侧栏按环节展示选题方向、素材参考、大纲参考、AI 修改、审核意见和配图概况；保留次级“写作设置”，排版导出保持原布局并移除无效开关。
- 展开素材、聚焦大纲章节可对照已有主张与来源；问题入口跨分类和页码定位，章节和配图导航定位主区。区分历史依据、未采用及已删除来源，不把关联视为核实。
- 按文章和环节记住标签与展开偏好；普通选择不抢标签、不强行展开。窄窗口改用可关闭抽屉，支持遮罩、Esc、焦点约束和返回；禁用浏览器存储时仍可操作。
- 写作设置使用最新文章数据逐项合并；切换环节、标签和关闭侧栏等待保存，失败保留输入和选择并可重试。主题统一在选题主区采用。
- 验证：193 项后端回归、类型检查与构建通过；隔离浏览器覆盖七环节、关联定位、排序删除、保存失败恢复、审核与选段修改、配图上传采用、复制及 ZIP、长内容和 1440／1100／390 窗口。原有 7 套主题在 375／430 阅读宽度及快速切换、预览失败、复制一致性、保存冲突验收通过。全部生成使用本地模拟，未调用付费模型。

- 实际后台与提供的界面已核对为 1.4.9；安全升级、同版本启动复用及真实文章只读界面检查通过，文章／历史／设置／凭证记录未变化。旧浏览器页面需刷新以加载新版侧栏。

# 1.4.8

- 按用户要求恢复“排版导出”的原始布局：主题下拉框、颜色圆点、直接显示的字号／行距设置及手机预览。移除主题卡片和分类界面。
- 新增典藏书页、雅致画廊、精密手册，章饰、双细线、几何角饰、编号签等随文章复制和导出；与已有主题一起放入原下拉选项，共 25 套。
- 修复实际浏览器旧标签页仍运行旧界面的使用路径：启动器用当前版本及唯一地址参数打开新页，保留原标签页；窄窗口也显示版本号，更新刷新同样使用新地址。
- 已在用户实际 Edge 窗口确认旧界面读取新后台的混合状态，并另开同篇文章的新版页面。实际文章的调用记录无预估金额、辅助检查为中文条目、阅读与结构建议名称正确。
- 190 项全量回归及后续 24 项专项复核通过；构建、恢复原布局后的浏览器验收、7 套主题在 375／430 阅读宽度的检查通过。实际升级与重复启动通过，升级期间文章、历史、设置与凭证记录未变化，未调用付费模型。

# 1.4.7

- 新增杂志长文、专业科普、人文随笔、现代专题 4 套完整排版主题；统一设计章节、引用、列表、表格、图片图注和文献区，保留 18 套经典主题。
- 用真实版式预览卡片选择主题，切换时应用推荐字号、行距和段距；支持微调与恢复推荐设置、新建文章默认专业科普，旧文章不批量变更。
- 手机预览、复制与 HTML / ZIP 使用相同正文渲染；主题装饰不写回原文，连续切换期间禁止复制旧结果。
- 原 AI 排版建议改为折叠的“阅读与结构建议（AI，可选）”，仅提供具体建议，不自动改写文章或应用主题。
- 验证：181 项全量回归及后续 16 项专项复核、前端类型检查与构建通过；浏览器覆盖 4 套主题的 375／430 阅读宽度、快速切换、保存冲突、预览失败重载、复制一致性和未保存正文。实际后台升级与重复启动核验通过，文章、历史、设置和凭证记录未变化，未调用付费模型。
- 公众号编辑器实际粘贴效果尚未验证，本地预览不等同于平台最终呈现。

# 1.4.6

- 修复更新代码后双击启动仍复用旧后台：启动器核对版本，闲置时切换新版；有生成任务时保留当前服务，提示任务结束后再启动。
- 页面显示版本，检测前后台及磁盘版本差异；提供明确更新提示，刷新新版页面前先保存正文。
- HTML 入口禁用缓存，避免继续加载旧界面；保留文章、历史版本及凭证。
- 验证：168 项后端回归、前端类型检查与构建通过；实际启动升级与重复启动、实际界面的调用记录和审核详情、保存后刷新及保存失败保护均通过。文章、历史与凭证记录未变化，未调用付费模型。

# 1.4.5

- 取消配图与检索金额门槛；调用记录保留用量与状态，移除预估金额。
- 按主张评估出处质量，复用原文证据并选择长材料相关片段；补齐书籍引用，保留独立研究与不同版本。
- 审核意见全部处理后同步结束本轮，区分人工处理与 AI 通过；辅助检查改为中文可读条目。
- 统一排版进度中文提示；预览、复制和导出不再自动添加 AI 创作声明。
- 验证：155 项后端回归、前端类型检查与构建、隔离浏览器验收通过；覆盖审核处理与刷新、未保存正文、无价格生图、书籍引用、排版进度及复制内容。本次使用模拟服务，未调用付费模型。

# 1.4.4

## 恢复上一版与逐张上传

- 恢复 v1.4.3 的功能与配图界面，撤回 v1.5.0 的配图扩展；以新提交保留完整历史，不回滚或覆盖本机数据库。
- 每个配图方案的“生成这一张”旁增加“上传图片”，自动沿用该方案的封面／插图用途、图注和章节位置；仍可在图片卡片中修改。
- 上传不需要启用 AI 或配置生图服务；保留已有图片、顶部上传入口及文章版本冲突保护。
- 136 项后端回归、前端构建通过；隔离浏览器验收覆盖逐张上传、重复选择同一文件、顶部上传不串用方案、刷新恢复与两种窗口尺寸。未调用付费模型。

# 1.4.3

## 简化素材使用方式

- 移除独立 AI 用途的生成字段、自动保存、有效期判断和页面展示。模型结合现有证据与大纲决定材料如何使用。
- 保留人工填写的“使用要求（可选）”，默认随素材详情折叠；支持清空，清空后不回退到旧 AI 用途。
- 素材详情展示已有的关联主张和大纲章节；明确区分历史关联与当前结果，不把大纲关联等同于正文已引用。
- 个人经历授权继续独立、显式确认。使用要求不能把未核实内容变成事实，也不能代替亲历授权。
- 保留历史数据中的旧字段，但从模型上下文中过滤，不批量改写正式文章或历史版本。

## 验证

- 132 项后端回归通过；浏览器覆盖大量素材、分页、批量采用／忽略、旧字段隐藏、人工要求编辑与清空、关联章节、问题恢复和两种窗口尺寸。
- 原有立即导出、归档复用和文献导入保持通过。真实隔离调用完成分析、大纲、正文，共 3 次调用；人工要求保留，旧 AI 用途未进入模型，正式资料与配置不变。
- 本次验证的是功能与兼容性，不据此宣称文章质量提升。既有依赖提示保留，未升级依赖。

# 1.4.2

## 素材整理与交互

- 素材页分为整理结果和本篇素材两个页签；顶部保留采用数量、待处理／待复核数量及资料状态。按文章记住页签、筛选、页码和卡片展开情况。
- 素材默认显示两行摘要与简短用途，每页 10 条；支持搜索、采用状态筛选、当前页展开／收起，以及作用于全部筛选结果的批量采用／不采用。
- 核实问题按需要处理、写作局限、已处理分类，卡片可展开，每页 10 条；支持全选当前页、批量核实和忽略，保留撤销、补充资料及原任务恢复。
- 暂停提示定位问题，补充材料后可返回对应问题；浏览操作不保存文章，也不触发模型请求。
- AI 在既有资料核对／分析调用中生成用途，人工指定优先且不被覆盖；可恢复 AI 判断。主题和材料变化后旧用途停止应用，排版和执行节奏变化不影响用途。
- 个人经历授权单独显示，由用户明确开启。上传论文、范文或他人叙述不会自动成为作者亲历。

## 验证与兼容

- 132 项后端回归通过，前端构建及启动、重复启动、端口占用切换检查通过。既有依赖提示保留，未升级依赖。

- 新增 AI 用途为派生字段，兼容旧资料和旧模型输出；不导致资料整理结果自我失效，也不改变数据库结构。
- 隔离浏览器覆盖 0、1、30、100 条素材、批量操作、版本冲突、恢复入口和两种窗口尺寸；原有大纲恢复、文献导入、立即导出、归档复用继续验证。
- 真实隔离验证完成素材分析、大纲与正文，共 3 次模型调用；用途随分析生成，没有额外分类调用，人工用途保留且个人经历未自动授权。正式文章、配置和凭证未改写。
- AI 用途是写作建议，不等于事实已经核实；缺少用途字段的旧结果显示待整理，在下次资料整理时补充。浏览位置保存在当前浏览器会话，关闭会话后恢复默认视图。

# 1.4.1

## 工作流修复

- 资料核对暂停使用独立的待处理状态，保留目标环节和具体原因；不再把没有生成大纲的任务显示为成功。
- 后端统一提供各环节执行条件；导航可查看，生成操作在前置条件不足时弱化并提供处理入口。手写、导入、审核和排版继续支持按需使用。
- 核实问题支持逐项和批量核实、忽略并保留边界、撤销及关联补充材料。材料变化后重新评估；处理完成可恢复原任务，重复点击不会重复启动。
- 区分核心证据缺口与写作局限，并核对问题是否超出用户原始写作目的。忽略和一般局限均不标为已证实；找不到任何原文依据时仍需处理。
- 已核实资料可直接复用到大纲；大纲变化或审核新正文时按需检查。AI 核实成功同步更新素材状态，自动恢复后页面跟随实际环节。
- 本次与历史记录分别展示；已有材料、缓存、搜索、读取尝试、全文/摘要和元数据查询分别计数。旧计数明确标注统计限制。
- 网页引文支持可追溯的空白格式等价，PDF 保留抽取格式兼容；改写和无来源引文仍不能通过。
- 结构化结果允许附带说明文字，但只接受唯一、完整、符合约定的 JSON 对象；不通过额外付费请求修复格式。

## 验证与兼容

- 最终后端完整回归 124 项通过，前端构建通过；网页空白匹配另有针对性回归。
- 隔离浏览器通过暂停、忽略、撤销、补充、核实、恢复大纲及刷新检查；1366×768、1280×800 布局无横向溢出或脚本错误。
- 原有写作至 ZIP 下载、导出前保存、归档复用、引用和文献导入检查保持通过。
- 真实 API 验证在本机隔离副本执行，报告、生成文章及图片均不进入公开仓库。人工流程已覆盖大纲、正文、审核、配图与 ZIP；审核提出的修改意见如实保留，不冒充事实审核通过。
- 真实自动流程覆盖选题、联网、素材、大纲、初稿和限轮审核；审核仍有措辞及篇幅意见时正确暂停。人工采纳意见后续行完成真实配图及 ZIP，6 条引用全部可关联，图片链接和归档字节一致；不把可导出等同于审核通过或无人干预全程通过。
- 保留现有数据库、素材地址、引用编号和历史版本；新增状态和 JSON 字段兼容旧记录，无需清空数据。

## 限制

模型的核实和写作范围判断仍可能出错，需结合原文与审核意见使用。真实测试中发现并修复了带前置说明的 JSON 解析失败，也遇到了上游模型流中断及确实损坏的 JSON（后者严格拒绝）；未知费用和失败记录保留，测试改用另一已配置模型时仅调整隔离配置。依赖弃用提示及编辑器包体积提示维持现状，本次未升级依赖。

# 1.4.0

## 改动

- 导出先保存编辑，再生成完整归档；正文、HTML、图片、来源清单和 ZIP 使用同一快照。重复下载复用已有快照，改稿和改标题保留旧版。
- 成品、诊断产物与隔离测试目录分开；日志和浏览器缓存归入 `data/` 子目录。已知旧产物提供可重复执行的整理工具。
- 能力测试按“服务＋模型”显示，包含节点覆盖模型；文本、图片、联网分别记录结果。独立联网协议不会更改文本协议或创作分工。
- 检索默认模型优先，按证据缺口使用已启用的后备；资料足够就停止，不再固定追加学术检索。保留原文读取、引用核对、预算、缓存与取消保护。
- 素材页统一“查找并整理资料”，合并总结，折叠过程记录，明确过期状态。BibTeX / RIS 使用统一上传入口。
- 搜索配置移至流程偏好，网页搜索与动态网页读取分开控制。移除工作台搜索测试入口和人工验证浏览器流程。

## 验证（2026-09-18）

- 后端 108 项测试通过：模型独立测试、配置失效、搜索回退与限额、材料排除、取消、冲突、版本恢复、引用、归档复用及失败等。
- 前端构建通过；保留预构建 `dist/`，日常双击启动无需 Node.js。未升级项目依赖。
- 模拟界面验证：6 张卡片共 18 项能力测试；1366×768 和 1280×800 布局；资料整理至写作、审核、导出；未保存编辑导出、重复归档、刷新恢复、BibTeX 导入及过期提示。
- 独立 Edge 浏览器成功下载 ZIP，文件内容及版本名称正确，无页面脚本错误。保留用户下载管理器设置。
- 真实调用另行验证：当前联网配置取得来源并读取、核对原文；当前图片配置实际生成一张图片。测试在隔离工作区运行，未把模拟结果登记为正式模型能力。
- 启动、重复启动复用、停止、端口占用回退和重启通过；重启前后正式文章、历史版本和凭证内容一致。

## 保留的限制

- 真实验证仅覆盖本机当时选用的联网和图片配置，不代表所有服务、模型或渠道均已通过真实调用。网站访问限制和第三方服务状态仍可能影响结果。
- 云服务可能收费；费用未知时不视为免费。付费请求中断或结果未知时不会自动重复请求。
- 测试仍有依赖弃用及模拟配置序列化提示，构建仍提示编辑器包较大；本次未为消除提示升级依赖或调整编辑器架构。
- IDM 等工具可能接管浏览器下载；成功导出的文件同时保存在本机归档目录。单独 HTML / Markdown 需配套图片目录，完整作品建议使用 ZIP。

本机文章、素材、凭证、测试截图及报告均未纳入公开仓库。
