# 1.4.1

## 工作流修复

- 资料核对暂停使用独立的待处理状态，保留目标环节和具体原因；不再把没有生成大纲的任务显示为成功。
- 后端统一提供各环节执行条件；导航可查看，生成操作在前置条件不足时弱化并提供处理入口。手写、导入、审核和排版继续支持按需使用。
- 核实问题支持逐项和批量核实、忽略并保留边界、撤销及关联补充材料。材料变化后重新评估；处理完成可恢复原任务，重复点击不会重复启动。
- 区分核心证据缺口与写作局限，并核对问题是否超出用户原始写作目的。忽略和一般局限均不标为已证实；找不到任何原文依据时仍需处理。
- 已核实资料可直接复用到大纲；大纲变化或审核新正文时按需检查。AI 核实成功同步更新素材状态，自动恢复后页面跟随实际环节。
- 本次与历史记录分别展示；已有材料、缓存、搜索、读取尝试、全文/摘要和元数据查询分别计数。旧计数明确标注统计限制。
- 网页引文支持可追溯的空白格式等价，PDF 保留抽取格式兼容；改写和无来源引文仍不能通过。
- 结构化结果允许附带说明文字，但只接受唯一、完整、符合约定的 JSON 对象；不通过额外付费请求修复格式。

## 验证与兼容

- 最终后端完整回归 124 项通过，前端构建通过；网页空白匹配另有针对性回归。
- 隔离浏览器通过暂停、忽略、撤销、补充、核实、恢复大纲及刷新检查；1366×768、1280×800 布局无横向溢出或脚本错误。
- 原有写作至 ZIP 下载、导出前保存、归档复用、引用和文献导入检查保持通过。
- 真实 API 验证在本机隔离副本执行，报告、生成文章及图片均不进入公开仓库。人工流程已覆盖大纲、正文、审核、配图与 ZIP；审核提出的修改意见如实保留，不冒充事实审核通过。
- 真实自动流程覆盖选题、联网、素材、大纲、初稿和限轮审核；审核仍有措辞及篇幅意见时正确暂停。人工采纳意见后续行完成真实配图及 ZIP，6 条引用全部可关联，图片链接和归档字节一致；不把可导出等同于审核通过或无人干预全程通过。
- 保留现有数据库、素材地址、引用编号和历史版本；新增状态和 JSON 字段兼容旧记录，无需清空数据。

## 限制

模型的核实和写作范围判断仍可能出错，需结合原文与审核意见使用。真实测试中发现并修复了带前置说明的 JSON 解析失败，也遇到了上游模型流中断及确实损坏的 JSON（后者严格拒绝）；未知费用和失败记录保留，测试改用另一已配置模型时仅调整隔离配置。依赖弃用提示及编辑器包体积提示维持现状，本次未升级依赖。

# 1.4.0

## 改动

- 导出先保存编辑，再生成完整归档；正文、HTML、图片、来源清单和 ZIP 使用同一快照。重复下载复用已有快照，改稿和改标题保留旧版。
- 成品、诊断产物与隔离测试目录分开；日志和浏览器缓存归入 `data/` 子目录。已知旧产物提供可重复执行的整理工具。
- 能力测试按“服务＋模型”显示，包含节点覆盖模型；文本、图片、联网分别记录结果。独立联网协议不会更改文本协议或创作分工。
- 检索默认模型优先，按证据缺口使用已启用的后备；资料足够就停止，不再固定追加学术检索。保留原文读取、引用核对、预算、缓存与取消保护。
- 素材页统一“查找并整理资料”，合并总结，折叠过程记录，明确过期状态。BibTeX / RIS 使用统一上传入口。
- 搜索配置移至流程偏好，网页搜索与动态网页读取分开控制。移除工作台搜索测试入口和人工验证浏览器流程。

## 验证（2026-09-18）

- 后端 108 项测试通过：模型独立测试、配置失效、搜索回退与限额、材料排除、取消、冲突、版本恢复、引用、归档复用及失败等。
- 前端构建通过；保留预构建 `dist/`，日常双击启动无需 Node.js。未升级项目依赖。
- 模拟界面验证：6 张卡片共 18 项能力测试；1366×768 和 1280×800 布局；资料整理至写作、审核、导出；未保存编辑导出、重复归档、刷新恢复、BibTeX 导入及过期提示。
- 独立 Edge 浏览器成功下载 ZIP，文件内容及版本名称正确，无页面脚本错误。保留用户下载管理器设置。
- 真实调用另行验证：当前联网配置取得来源并读取、核对原文；当前图片配置实际生成一张图片。测试在隔离工作区运行，未把模拟结果登记为正式模型能力。
- 启动、重复启动复用、停止、端口占用回退和重启通过；重启前后正式文章、历史版本和凭证内容一致。

## 保留的限制

- 真实验证仅覆盖本机当时选用的联网和图片配置，不代表所有服务、模型或渠道均已通过真实调用。网站访问限制和第三方服务状态仍可能影响结果。
- 云服务可能收费；费用未知时不视为免费。付费请求中断或结果未知时不会自动重复请求。
- 测试仍有依赖弃用及模拟配置序列化提示，构建仍提示编辑器包较大；本次未为消除提示升级依赖或调整编辑器架构。
- IDM 等工具可能接管浏览器下载；成功导出的文件同时保存在本机归档目录。单独 HTML / Markdown 需配套图片目录，完整作品建议使用 ZIP。

本机文章、素材、凭证、测试截图及报告均未纳入公开仓库。
