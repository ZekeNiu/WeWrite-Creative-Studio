# 项目交付约定

- 用户要求：每个版本完成后都要上传 GitHub，不以仅完成本地修改作为版本交付。
- 发布前同步应用版本、锁文件、后端版本与 CHANGELOG；前端变动要构建并提交 dist。
- 完成适合本次改动的检查；本任务中已通过且代码未改变的检查无需重复执行。
- 将本版本提交、版本标签与 GitHub Release 上传到当前仓库的 origin，并核对远端提交与发布结果。不要覆盖既有标签或强制推送。
- 仅发布项目代码、文档、预构建界面和模拟测试；data、output、凭证、个人文章与本地诊断记录不得提交。
- 用户要求暂缓上传或限制交付范围时，以用户当前指示为准。
