# WeWrite 本地创作工作台

双击 **启动工作台.bat**，浏览器会自动打开。关闭网页不会停止后台；要退出后台，双击 **停止工作台.bat**。重复启动会打开同一个工作台。

## 第一次使用
1. 打开右上角“AI 服务与设置”，添加服务。调用地址已预填 `https://api.apikey.fan`。
2. 填入自己的 API Key 和准确的模型名称，按接入文档选择预设，保存并测试文本连接。协议可在高级设置中调整；分组由 Key 绑定。
3. 在“节点分工”选择共享默认服务。可单独为选题、写作、审核、修改等指定不同模型。
4. 在“能力测试”逐个模型验证文本、图片或联网能力。联网方式在对应卡片内设置，再到“流程偏好”选择实际联网模型；独立测试不会更改节点分工。
5. 回到文章库，新建文章。选择常用栏目，也可输入任意领域或指定主题。

## 创作
- 每个环节默认等待确认。点击该环节的生成按钮后才会调用 AI。打开“自动继续”，完成当前环节后继续处理下一个环节，直到遇到确认点或问题。
- 指定主题后不需要再生成选题；选题卡片中的“就写这个”会确认该题。
- 素材支持 PDF、DOCX、Markdown、TXT、网页与文字，以及 BibTeX / RIS 文献导入；扫描 PDF 需先转成可提取文字的 PDF。
- 自己提供的材料也可能是第三方参考文献；只有勾选“这是我的真实经历”的材料才允许写成作者亲历。
- 大纲可编辑、拖动排序或单独重做章节。正文支持富文本编辑及 Markdown 源文。
- 选中文字，在右侧提出修改要求；先看建议再应用。复杂跨段选区如无法精确匹配，请选择单段或用 Markdown 视图选取原文。
- 审核意见可逐条应用或保留原文；应用修改后请复审。模型“审核通过”不等于外部事实已独立验证。
- 更改前面的内容，会把相关后续环节标成“需要更新”，不会删除旧结果。历史版本可恢复；恢复本身也会保存一个新版本。
- 所有文字自动保存。切换环节前会等待正文保存完成。

## 自动检索与证据
- 在“流程偏好”选择联网模型。创作时先检查已有材料，模型优先查找缺失依据；证据足够就继续，不再固定补查所有渠道。
- “允许使用后备搜索”开启时，模型失败或依据不足才尝试已启用的 Tavily、网页搜索。Tavily 可不配置。
- 在高级设置中控制学术补查、网页后备搜索、动态网页读取和限额。网页搜索与原文读取分别控制。论文补查仅用于确实缺少研究依据的问题。
- 模型联网接入与文本协议独立：Responses、Anthropic Messages 或 Gemini Google Search；Chat Completions 文本连接成功不等于联网工具已接通。未独立测试但接入完整的模型可在实际任务中尝试。
- 搜索成功必须有实际工具记录和来源；普通文本回答“已联网”不算通过。读取与引用核对另行进行，摘要和全文分别标记。
- 在“素材”点击 **查找并整理资料**，可补充检索要求，也可直接上传材料。结果与待补问题集中展示，详细渠道及日志默认折叠。
- 受限网站优先寻找替代来源；必要时通过上传文件、添加链接或粘贴原文补充，不再要求操作专门的验证浏览器。
- 移除或排除的材料不会自动导回。材料或主题改变后，旧整理结果保留为历史，需重新整理才能使用。
- 搜索与配图不设金额预算门槛，价格可不填写；搜索次数、页面及补查轮数上限继续生效。结果未知的请求不在同一任务中自动重放。“调用记录”保留环节、模型、用量、状态和时间，不显示预估金额。
- 按具体主张选择适合的出处：研究结论优先论文、综述、专业书籍与权威机构；技术实践优先官方文档、原始研究和有依据的专业博客。维基百科用于背景梳理，关键结论尽量追溯原始出处。
- 引用详情展示来源类别、采用理由和适用范围；旧素材未评估的字段可为空。摘要、预印本、搜索片段及仅书目的材料分别保留边界，不把“引用定位成功”当成事实已证实。
- 长材料保留原文，按研究问题、大纲和已定位证据选取片段，包含相关上下文；输入注明截取范围。重复出处复用引用编号，独立研究、不同版本与反方证据分别保留。
- 每张模型卡片有独立的文本、图片、联网测试结果。更换地址、模型、凭证或相关协议后，旧结果失效；只保存设置不会触发测试。


## 配图
- AI 配图默认关闭。上传自己的封面和插图不需要开通生图服务。
- 每个方案的“生成这一张”旁都有“上传图片”，可直接上传自备图片，并继承该方案的用途、图注和章节位置。上传后仍可修改，不覆盖已有图片。
- 需要生成图片时，配置图片模型即可，单价可选，币种不影响生成。勾选自动继续的配图环节会依次生成方案中的图片；手动模式可单张生成。
- 能力测试按服务与模型分别显示卡片；文本、图片、联网测试均针对所点卡片，不改变节点分工。
- “测试图片生成”实际生成 1 张并显示预览；测试和正常文章生图均不要求填写价格或预算。
- 图片生成失败、停止或超时会保留调用记录，不自动重新请求。
- 图片可设置封面 / 内文用途、图注和章节位置。重新生成的图片不会覆盖此前图片。

## 引用与参考文献（1.3.0）
- 正文中的来源标记显示为上标，按首次出现顺序编号；同一来源复用编号。移动、删除、撤销及版本恢复后自动重排。
- 编辑器点击“插入引用”选择素材，点击上标或素材标题可查看证据并修正文献信息。选中旧稿的普通数字编号后再插入引用，即可手动关联；程序不会猜配。
- 素材中的 DOI 可用于从 Crossref 核对信息。BibTeX / RIS 只导入文献记录，未读到正文时不当作研究证据。
- 按正式 GB/T 7714—2025 的期刊论文、会议论文、预印本和网页规则生成条目；预印本使用 PP。信息不足、姓名顺序不明或尚不支持的类型会提示待补全，不能视为已规范化。
- 书籍使用 M 类型，可编辑作者、出版地、出版社、年份、版本和引用页码；BibTeX/RIS 导入保留这些信息。仅导入书目不能作为书中结论的证据。
- 文末“参考文献”由程序生成，只列实际引用的来源，并放在配图与署名之后。原稿中手写的文献表不会被自动删除，建议在关联旧引用后手动整理。

## 审核与交付
- 辅助评分与机械检查以中文条目展示，仅辅助判断表达，不代表事实准确率或 AI 检测概率。
- 逐项接受修改或保留原文，全部处理后显示“本轮意见已处理”，无需再次确认，不自动复审或启动后续环节。这与“AI 审核通过”分别显示；之后正文或相关依据变化，审核需要更新。
- 排版预览、复制与各类导出不再自动追加 AI 创作或配图声明。用户主动写入正文的文字和以前导出的归档不会批量修改。
- 学术检索会合并同一论文的发现渠道和全文地址；摘要升级全文后保留内部来源编号。BibTeX / RIS 使用素材页面统一上传入口导入。
- 搜索设置统一位于“流程偏好”，可填写 OpenAlex 免费 Key、调整渠道与限额。

## 排版与导出
本地排版提供 18 种主题，不调用 AI。字体、行距、段落间距与署名可以实时调整。

“复制公众号排版”同时复制带上标的富文本和普通编号纯文本，二者均含文献表。Markdown 导出保留可渲染的 `<sup>` 标记。浏览器剪贴板失败时，可先下载 HTML。本地图片不能保证随剪贴板进入微信，请在微信编辑器里按图示位置上传图片。

含图片的作品请下载 ZIP：其中有文章.md、排版.html、images 图片目录和来源清单。单独下载 Markdown 或 HTML 时，图片使用相对路径，需要同时保留图片目录。

## 文件与隐私
- `data/`：文章数据库、版本、凭证密文、素材与图片。备份这个目录即可保存创作数据。
- `dist/`：已构建页面，日常使用不需要 Node.js。
- `vendor/wewrite/`：固定版本的 WeWrite 源码、规则及 MIT 许可。
- 工作台独立保存设置，不会自动读取其他项目的凭证。
- API Key 使用 Windows 当前用户加密，不写入文章和导出文件；换电脑或换 Windows 用户后应重新填写 Key。
- 应用只监听本机地址。调用 AI 或搜索时，相应内容会发送到你配置的服务；“本地”表示工作台和数据保存在本机，不表示云模型离线运行。

## 常见问题
- **启动后没有打开页面：** 等待首次依赖检查，查看 `data/logs/setup.log` 和 `data/logs/server.log`；确认 Python 3.11+ 已安装。
- **无法访问模型：** 检查地址、协议、模型名、Key 分组及额度。文本和生图分别测试。
- **没有 Tavily Key：** 无需购买。模型联网可独立使用；允许后备时，会按证据缺口使用已启用的网页搜索，确需研究依据时再查学术库。
- **部分搜索页需要验证：** 程序会寻找替代来源；仍缺关键依据时，可通过上传或粘贴补充材料。
- **生成中改了正文：** 过期结果保留在生成记录中，不覆盖新编辑；刷新并决定是否重做。
- **程序关闭后任务中断：** 内容仍保留，重新打开后手动从该环节恢复，不自动重复付费请求。

## 开发与测试
前端：`npm ci`、`npm run build`。后端：项目独立环境安装 `requirements-lock.txt`。后端测试：`.venv\Scripts\python.exe -m pytest tests -q`。

WeWrite 固定为 4.2.1 / e8df474ea8d9fd1b06b474115cee568dc46c9ab2。API 兼容性和真实写作质量需要使用本人服务配置验证；离线模拟测试不等于真实模型验收。

## 导出归档与目录
- 下载前会等待当前编辑保存，生成完整快照。相同版本及内容重复下载复用归档，旧版本不被覆盖。
- `output/articles/<文章ID>/<日期时间>_<标题>_r<版本号>/` 保存正文 Markdown、排版 HTML、图片、来源清单、说明和 ZIP；页面显示保存位置并提供“打开归档文件夹”。
- 下载名称采用 `<标题>_<日期时间>_r<版本号>`。完整图片请使用 ZIP，单独下载的 Markdown、HTML 需与归档内的 `images/` 一起使用。
- `output/diagnostics/` 保存测试报告、截图和能力测试图片；`output/test-workspaces/` 保存隔离测试数据。
- 数据库与用户素材仍在 `data/`，日志在 `data/logs/`，动态网页读取使用 `data/cache/`。旧测试图片地址兼容保留。
- 磁盘空间或权限导致归档失败时会明确提示，已有快照和文章不受影响。
- GitHub 仅保存程序与文档，不包含本机文章、上传材料、凭证、日志、截图和测试输出。备份个人创作数据请同时备份 `data/` 与 `output/articles/`，不要上传到公开仓库。

使用 IDM 等下载管理器时，浏览器下载可能被接管；无论是否接管，成功导出的完整文件已保存在上述归档目录，可直接打开文件夹获取。

## 工作流暂停与核实（1.4.1）
- “已完成”“待你处理”“失败”和“已停止”分别展示。大纲在资料核对阶段暂停时，会明确说明尚未生成大纲。
- 每个核实问题可选择 AI 核实、忽略并继续、补充资料；勾选多个问题可批量处理。忽略可撤销，表示保留边界或省略未证实断言，不代表事实已证实。
- 补充资料复用上传、链接及粘贴入口。上传成功后仍需核实；材料或写作要求变化后，旧决定会重新评估。
- 一般研究局限保留到文章中。检索器自己扩展的方法学问题不会自动成为文章必须完成的任务；没有任何可定位依据时仍需要补充或明确选择保留边界后继续。
- 人工模式处理后点击“继续生成大纲”等按钮；自动模式按原任务设置继续。刷新不会重放请求，失败后的重试需要主动点击。
- 前置条件不足时，导航弱化但可查看已有内容，生成按钮禁用并显示处理入口。手写和导入正文仍可直接进入审核与排版，AI 配图为可选环节。
- “本次资料核对”分开记录已有材料、新增搜索、缓存复用、读取尝试、全文/摘要及文献信息查询；“历次检索与核对”保留各次记录。旧版计数不会伪装成成功读取篇数。
- 大纲复用有效素材；新增要求或大纲变化时按需补查。审核针对当前正文判断；审核提出修改不等于通过，已有稿件和导出能力仍保留。

## 1.4.2 素材页使用

- 在“资料整理结果”和“本篇素材”之间切换；卡片默认收起，每页 10 条。浏览、筛选和折叠不会修改文章或调用模型。
- “全部采用／全部不采用”作用于全部筛选结果，包含其他页；问题的“全选当前页”仅用于批量核实或忽略。
- “使用要求（可选）”仅供人工填写特殊要求，留空即可。素材展开后可查看已有的关联主张和大纲章节，旧版 AI 用途不再展示或使用。
- “个人经历授权”由你明确开启，上传参考资料不等于你的经历。
- 从暂停提示进入可定位问题，补充资料后可返回对应问题继续核实。
