# WeWrite 本地创作工作台

双击 **启动工作台.bat**，浏览器会自动打开。关闭网页不会停止后台；要退出后台，双击 **停止工作台.bat**。重复启动会打开同一个工作台。

## 第一次使用
1. 打开右上角“AI 服务与设置”，添加服务。调用地址已预填 `https://api.apikey.fan`。
2. 填入自己的 API Key 和准确的模型名称，按接入文档选择预设，保存并测试文本连接。协议可在高级设置中调整；分组由 Key 绑定。
3. 在“节点分工”选择共享默认服务。可单独为选题、写作、审核、修改等指定不同模型。
4. 在“能力测试”点击“测试工作台搜索”。模型联网测试是另一项可选能力，可独立选择 Responses、Anthropic 或 Gemini Google Search 接入方式，不改变文本协议。Tavily 可不配置。
5. 回到文章库，新建文章。选择常用栏目，也可输入任意领域或指定主题。

## 创作
- 每个环节默认等待确认。点击该环节的生成按钮后才会调用 AI。打开“自动继续”，完成当前环节后继续处理下一个环节，直到遇到确认点或问题。
- 指定主题后不需要再生成选题；选题卡片中的“就写这个”会确认该题。
- 素材支持 PDF、DOCX、Markdown、TXT、网页与文字，以及 BibTeX / RIS 文献导入；扫描 PDF 需先转成可提取文字的 PDF。
- 自己提供的材料也可能是第三方参考文献；只有勾选“这是我的真实经历”的材料才允许写成作者亲历。
- 大纲可编辑、拖动排序或单独重做章节。正文支持富文本编辑及 Markdown 源文。
- 选中文字，在右侧提出修改要求；先看建议再应用。复杂跨段选区如无法精确匹配，请选择单段或用 Markdown 视图选取原文。
- 审核意见可逐条应用或保留原文；应用修改后请复审。模型“审核通过”不等于外部事实已独立验证。
- 更改前面的内容，会把相关后续环节标成“需要更新”，不会删除旧结果。历史版本可恢复；恢复本身也会保存一个新版本。
- 所有文字自动保存。切换环节前会等待正文保存完成。

## 自动检索与证据
- 在“AI 服务与设置 → 能力测试 → 联网搜索方式”选择：自动选择（默认）、模型联网优先、Tavily 优先或浏览器优先。保存一次后用于后续创作与工作台搜索测试。
- “首选方式失败或资料不足时，允许自动切换”默认开启。关闭后不会改用另外两种方式；浏览器内部仍可在 Google、Bing、百度、DuckDuckGo 之间切换。自动模式关闭切换时只选择首个可用方式。
- 指定方式会先于学术补查执行；默认自动模式延续原有学术优先逻辑。论文补查、原文读取及引用核对独立保留，仍受既有开关、次数与预算限制。没有可用配置会说明原因，不冒充所选方式已成功。
- 搜索结果与素材区会显示本次方式、是否允许切换、实际请求渠道及取得结果的渠道（含缓存）。改变搜索设置会使旧测试状态失效；仅保存不会产生搜索费用。
- 想使用已验证的 Gemini：选择“模型联网优先”，在“模型联网测试”中选择 Gemini 服务和 Gemini Google Search 接入方式，再保存。文本协议无需修改。
- 在“能力测试”点击 **测试工作台搜索**。分别显示检索、原文读取、AI 整理；需要有相关来源、可读取原文和逐字定位引用才通过。测试记录刷新后保留，测试资料不会放进文章列表。
- 模型无需自带联网能力。模型联网的接入方式独立于文本协议；“当前接入方式未验证”不影响工作台独立检索。不要为了搜索更改已经通过的文字模型协议。
- 论文发现：OpenAlex 跨学科检索，不可用时 Crossref 补位；运动健康补充 PubMed / PMC，AI 等领域补充 arXiv。普通网页顺序：已验证原生搜索 → 已配置且启用的 Tavily → Google → Bing → 百度 → DuckDuckGo。单个站点失效时自动切换，不绕过验证。
- 测试可选“本次不使用 Tavily”，只影响当次测试，不改日常设置。每次测试最多 5 次搜索、6 页读取，并遵守用户更低的限额；真实文本整理和已启用的搜索服务可能计费。
- 配置变化后旧测试失效；停止、失败或重启后不会自动重放付费请求。点击测试才启动新任务。
- 选题、素材、大纲、审核会自动检查资料缺口，已有材料优先复用；单纯存在素材不会跳过必要核对。
- “检索规划与资料整理”默认继承素材分析服务，也可单独指定模型；原生搜索服务在“联网搜索”中配置。
- 原生搜索必须取得真实工具记录与来源才标记已验证。普通 Chat Completions 不被默认当作支持搜索。
- 默认每个任务最多 8 次搜索调用、16 篇读取、2 轮补查；在联网搜索高级设置中调整。模型规划、整理消耗模型额度。
- 工具费用与模型 Token 分开记录；搜索工具预算仅约束工具费。设定金额后，价格未知或非人民币的付费渠道跳过；留空仍受次数限制。
- PubMed 的摘要记录明确标为摘要；存在开放全文链接时，会尝试实际读取全文，读取成功才标为原文。来源详情会展示逐字匹配的证据片段、位置及局限；片段匹配不等于独立事实验证。
- 关键冲突或证据不足会暂停到素材页。补充资料或修改主题后可重新运行；不会擅自覆盖已有稿件。
- 浏览器默认在后台使用独立环境，优先 Edge，再 Chrome；不接管日常标签或账户。网站验证码、付费墙不会绕过。
- 页面内容缓存最多 24 小时，选题检索结果较短；删除或排除的来源不会自动重新采用。
- 本机代理使用特殊 DNS 地址时，程序通过 Cloudflare DNS over HTTPS 核实公开域名；只允许公开网络地址，不能访问本机与内网服务。该查询不包含 API Key 或文章正文。

## 配图
- AI 配图默认关闭。上传自己的封面和插图不需要开通生图服务。
- 需要生成图片时，配置图片模型、人民币每张价格及本篇预算。勾选自动继续的配图环节会依次生成方案中的图片；手动模式可单张生成。
- 文本测试验证服务默认模型；图片测试直接使用“节点分工”里图片生成节点实际绑定的服务与模型。
- “生成测试图片”实际生成 1 张并显示预览，可能计费；未知价格明确显示“费用未知”，测试不受隐藏价格条件阻挡。正常文章生图仍需要人民币单价与预算。
- 图片生成失败、停止或超时可能仍被服务商计费，因此预留预算不会自动释放，也不会自动重新请求。
- 图片可设置封面 / 内文用途、图注和章节位置。重新生成的图片不会覆盖此前图片。

## 引用与参考文献（1.3.0）
- 正文中的来源标记显示为上标，按首次出现顺序编号；同一来源复用编号。移动、删除、撤销及版本恢复后自动重排。
- 编辑器点击“插入引用”选择素材，点击上标或素材标题可查看证据并修正文献信息。选中旧稿的普通数字编号后再插入引用，即可手动关联；程序不会猜配。
- 素材中的 DOI 可用于从 Crossref 核对信息。BibTeX / RIS 只导入文献记录，未读到正文时不当作研究证据。
- 按正式 GB/T 7714—2025 的期刊论文、会议论文、预印本和网页规则生成条目；预印本使用 PP。信息不足、姓名顺序不明或尚不支持的类型会提示待补全，不能视为已规范化。
- 文末“参考文献”由程序生成，只列实际引用的来源，并放在配图、署名与创作说明之后。原稿中手写的文献表不会被自动删除，建议在关联旧引用后手动整理。
- 学术检索会合并同一论文的发现渠道和全文地址；摘要升级全文后保留内部来源编号。Scholar 仅提供手动检索入口与文献导入，不要求用户平时打开。
- 搜索设置统一位于“能力测试 → 工作台搜索”下方，可填写 OpenAlex 免费 Key、调整渠道与限额。

## 排版与导出
本地排版提供 18 种主题，不调用 AI。字体、行距、段落间距与署名可以实时调整。

“复制公众号排版”同时复制带上标的富文本和普通编号纯文本，二者均含文献表。Markdown 导出保留可渲染的 `<sup>` 标记。浏览器剪贴板失败时，可先下载 HTML。本地图片不能保证随剪贴板进入微信，请在微信编辑器里按图示位置上传图片。

含图片的作品请下载 ZIP：其中有文章.md、排版.html、images 图片目录和来源清单。单独下载 Markdown 或 HTML 时，图片使用相对路径，需要同时保留图片目录。

## 文件与隐私
- `data/`：文章数据库、版本、凭证密文、素材与图片。备份这个目录即可保存创作数据。
- `dist/`：已构建页面，日常使用不需要 Node.js。
- `vendor/wewrite/`：固定版本的 WeWrite 源码、规则及 MIT 许可。
- 工作台独立保存设置，不会自动读取其他项目的凭证。
- API Key 使用 Windows 当前用户加密，不写入文章和导出文件；换电脑或换 Windows 用户后应重新填写 Key。
- 应用只监听本机地址。调用 AI 或搜索时，相应内容会发送到你配置的服务；“本地”表示工作台和数据保存在本机，不表示云模型离线运行。

## 常见问题
- **启动后没有打开页面：** 等待首次依赖检查，查看 `data/setup.log` 和 `data/server.log`；确认 Python 3.11+ 已安装。
- **无法访问模型：** 检查地址、协议、模型名、Key 分组及额度。文本和生图分别测试。
- **没有 Tavily Key：** 无需购买。原生搜索不可用时会自动尝试公开科研资料和本地浏览器。
- **部分搜索页需要验证：** 程序先换来源；确需处理时，在素材的“部分网页未能读取”中打开验证窗口，完成后点击“验证完成”，再“继续检索”。
- **生成中改了正文：** 过期结果保留在生成记录中，不覆盖新编辑；刷新并决定是否重做。
- **程序关闭后任务中断：** 内容仍保留，重新打开后手动从该环节恢复，不自动重复付费请求。

## 开发与测试
前端：`npm ci`、`npm run build`。后端：项目独立环境安装 `requirements-lock.txt`。后端测试：`.venv\Scripts\python.exe -m pytest tests -q`。

WeWrite 固定为 4.2.1 / e8df474ea8d9fd1b06b474115cee568dc46c9ab2。API 兼容性和真实写作质量需要使用本人服务配置验证；离线模拟测试不等于真实模型验收。
